# VSD-Paddock Cloud (Supabase) — progetto parallelo

## DECISIONE — Apps Script NON viene spento del tutto (24/09/2026)

Chiudendo #265 (cutover finale): Demetrio ha chiesto "possiamo tenere lo
script solo per il login?" dopo che la verifica live del 23/09 (fix
races-get) aveva mostrato che spegnere Apps Script oggi avrebbe rotto
l'intero sito, login compreso (vedi sotto). Risposta: sì, con perimetro
preciso — non è "solo login", è "auth + presence + 3 endpoint usati da
tool esterni mai ricollegati a Supabase":

- **`auth.discordStart`/`auth.discordCallback`** — login: OGNI utente,
  Demetrio incluso, autentica ancora tramite il backend Apps Script
  (`Login.jsx`/`AuthCallback.jsx`, mai portato a Supabase Auth
  nonostante Discord OAuth via Supabase esista da #176/#327, mai
  collegato al flusso reale).
- **`auth.verify`** — chiamato internamente da TUTTI i ~30 fallback
  `resolveLegacyDriver` nelle Edge Function Supabase per validare il
  legacy_token. Deve restare vivo finché quei fallback esistono (#341,
  vedi sotto).
- **`presence.heartbeat`/`presence.online`** — "chi è online" nel
  Roster, mai portato, legge ancora il foglio Drivers legacy.
- **3 tool esterni con l'URL Apps Script hardcoded come default, mai
  ricollegati alle Edge Function già pronte**: `companion/results_bridge.py`
  (raceResults.import), `companion/fuel_bridge.py` (fuel.logSample/
  logLive), `vsd-pitwall-bridge` C# (`pitwall.logSession` — **scoperta
  nuova**: il README di #340 dava per portato l'intero Pit Wall, ma solo
  `pitwall.broadcastLive` (live) è su Supabase; lo snapshot di fine
  sessione va ancora su Apps Script).

Il resto — tutte le Sheet, tutta la logica dati storica — non serve più
a nessuno in produzione. **Decisione**: Apps Script resta permanentemente
acceso come microservizio di autenticazione + ingestion companion, non
più come backend dati. #265 chiuso come "non applicabile" con questa
motivazione. #341 (rimozione fallback legacy) chiuso di conseguenza:
il fallback NON va più rimosso, è parte strutturale della decisione,
non un debito temporaneo.

**Due gap scoperti durante l'audit "cosa posso fare da UI/Supabase invece
di Apps Script", non ancora chiusi:**
1. **Creazione nuovo campionato** (`championships.add`/`championships.update`):
   Edge Function pronte e deployate da #252/#267, ma MAI collegate ad
   alcuna UI (nessun hook, nessun componente le chiama) — stesso identico
   gap del sorgente originale ("funzioni one-off eseguite dallo
   sviluppatore nell'editor"), solo spostato da Apps Script a "SQL diretto
   su Supabase". Se Demetrio deve creare un campionato nuovo, serve
   ancora un intervento diretto (via Supabase o chiedendo qui).
2. **Automazioni Discord basate su trigger time-driven** (`runBirthdayCheck`,
   `runWeeklyDigest`, `runSponsorFollowUpDigest`, `runRsvpReminderCheck`,
   `runSkillIndexSnapshot`, `runStintNotificationsCheck`,
   `runUpcomingRacePushCheck`, `runTeamSessionReminderCheck` in
   Notifications.js/Push.js/SkillIndex.js/Sponsors.js/RaceRSVP.js/
   TeamSessionsScheduler.js/SocialManager.js): leggono le Sheet legacy dei
   rispettivi domini, TUTTE congelate dal momento del cutover di quel
   dominio a Supabase (nessuna nuova riga arriva più lì). Sono quindi
   **già silenziosamente rotte o obsolete** indipendentemente da questa
   decisione — non generano più notifiche corrette (compleanni di piloti
   aggiunti dopo il cutover Roster, digest su dati Social Manager fermi,
   promemoria RSVP su gare create dopo il cutover Races, ecc.). Non
   toccate in questa sessione: da valutare se portarle su Supabase
   (richiederebbe pg_cron, mai fatto per nessun dominio finora) o
   accettarle come funzionalità perse.

Vedi `docs/ADR-multi-team-saas.md` (Opzione D) per il contesto completo della decisione.

## Cos'è

Un secondo backend, multi-tenant, su Supabase (Postgres + Auth + RLS + Realtime), pensato per rendere VSD-Paddock disponibile ad altri team come abbonamento web.

**Non è una migrazione**: `apps-script/` e il resto del repo restano invariati e continuano a servire VSD Virtual Sim Driver esattamente come oggi. Questo è un build nuovo, in una cartella separata, che non tocca né il foglio `VSD_HUB_DB` né i deployment Apps Script esistenti.

## Stato

**Fix cutover RaceRSVP/RaceCrews (#253) — 23/09/2026, scoperto durante audit generale "verifica cosa è necessario migrare ancora" richiesto da Demetrio.** Le 5 Edge Function di questo dominio (`rsvp-list`, `rsvp-set`, `race-crews-list`, `race-crews-add`, `race-crews-remove`) erano ferme alla versione originale di #253 (mai toccate da nessuna delle campagne di fix già applicate a ~25 altre Edge Function del progetto) e avevano ancora entrambi i bug tipici: (1) nessun fallback token legacy — 401 "Auth richiesto" per ogni pilota reale, dato che nessuno ha mai una sessione Supabase vera; (2) `driver_id` restituito come UUID interno invece del `driver_code` atteso ovunque nel frontend (`useRaceRSVP.js`/`useRaceCrews.js` → `RaceDetail.jsx`/`StintPlanner.jsx`/`AdminRaceStints.jsx`/`AdminHome.jsx`, confrontano `driver_id` contro `drivers` che usa `driver_code` — stesso identico pattern già fixato in best-laps-list e altri ~6 domini). Il sorgente locale (`cloud/functions/rsvp-list/`, `rsvp-set/`, `race-crews-list/`, `race-crews-add/`, `race-crews-remove/`) non esisteva nel repo git (drift già documentato per altri domini, es. #335/#337) — ricreato ora insieme al fix. Aggiunto lo stesso `resolveLegacyDriver` fallback usato ovunque + join `drivers!*_driver_id_fkey(driver_code)` in lettura; `race-crews-add` ora risolve `payload.driver_id` (che per contratto è il `driver_code`) sullo UUID interno PRIMA di ogni query, mai passato direttamente al DB. Deployate tutte e 5 in v7, poi collegate in `SUPABASE_MIGRATED_ACTIONS`/`LEGACY_TOKEN_FALLBACK_ACTIONS`. Nessuna azione richiesta ai piloti (stesso principio del fix Lookups sopra).

**Fix `races-get` senza fallback legacy token — 23/09/2026, scoperto validando live via Chrome dopo il push di Demetrio, richiesto esplicitamente da "adesso puoi verificare".** `races-get` era rimasta l'UNICA Edge Function del dominio Races esclusa dal fix #375 (`races.list`/`races.upcoming`/`raceResults.list`/ecc. già coperte) — ogni pagina `/race/:id` del sito (click su una gara da Race Hub) mostrava "Gara non trovata" per QUALSIASI utente reale, incluso Demetrio stesso: confermato via `localStorage` nel browser live che nemmeno l'admin ha mai una sessione Supabase vera (solo `vsd_paddock_token` legacy — stesso esito già documentato in #376), quindi la funzione riceveva solo l'anon key come Authorization, falliva `auth.getUser()` e rispondeva 401 "Auth richiesto"; il frontend (`RaceDetail.jsx`) interpreta silenziosamente qualunque risposta senza `race` come "gara non trovata", mascherando l'errore reale. Diagnosticato con una chiamata `fetch()` diretta eseguita nella pagina live (via `javascript_tool`), non tramite ispezione network — `read_network_requests` non ha mai catturato le POST reali verso le Edge Function in questa sessione. Fix: aggiunto lo stesso `resolveLegacyDriver` fallback delle altre funzioni del dominio, più scoping esplicito `.eq('team_id', me.team_id)` sulla query (necessario perché il client service-role del fallback bypassa la RLS, stesso principio già applicato in `races-list`). Deployata v8, aggiunta a `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js`. Nessuna azione richiesta ai piloti.


In costruzione. Fondamenta (teams/drivers/RLS), dominio Roster (#177), dominio Calendario/TeamSessions (#178/#242) e dominio Best Laps/Academy (#179) validati end-to-end in produzione. Dominio Pit Wall (#180) portato — schema `010_pitwall_sessions.sql` (`pitwall_sessions`) + 3 Edge Function (pitwall-log-session, pitwall-sessions-list, pitwall-session-get) deployate e validate end-to-end via chiamate dirette: matching nome-esterno→driver_id (algoritmo di `matchDriverName_` replicato), dedup/upsert per (session_id, driver_key), ordinamento classifica. Nessuna pagina preview dedicata costruita per Best Laps/Pit Wall — la validazione è passata per chiamata diretta alle Edge Function, come da precedente su Calendario. `academy.ranking` (#252), `laps.raceLaps` (#261) e `lapSubmissions.*`/`laps.syncFromGarage61` (#262) — inizialmente esclusi da questa fase — sono stati tutti portati successivamente, vedi note sotto. Il relay Realtime per viewer remoti (parte "live" di Pit Wall, distinta dallo snapshot di fine sessione) NON è stato portato in questa fase: richiede un cambio architetturale sul bridge C# (push di frame in tempo reale su un canale Supabase Realtime, non solo un JSON REST di fine sessione) più una nuova vista frontend — non è una semplice porting di endpoint esistenti, va scoping a parte, tracciato come #246/#263. Le notifiche Discord (nuovo record best lap, ecc.) sono state portate nei domini successivi (vedi #262). Nessun team reale (nemmeno VSD) è ancora su questo stack — Apps Script resta l'unico backend in produzione per il sito pubblico.

**Fase 2 — Championships + Standings + Academy + SeasonRecap + SkillIndex (#252) — fatto**, validato end-to-end. Schema `018_championships.sql` (`championships`, PK composita `team_id, id` perché lo slug non è univoco tra team; FK aggiunte a `races.championship_id`/`incident_reports.championship_id`, chiudendo il gap "nessuna FK" documentato in quei domini; `skill_index_history` per lo storico) + `019_races_race_number.sql` (colonna `race_number` mancante dal porting iniziale di Races, scoperta leggendo Standings.js — vedi sotto) + 12 Edge Function: `championships-list/add/update`, `standings-by-championship/progression/by-driver`, `championships-import-standings/save-adjustments`, `academy-ranking`, `season-recap`, `skill-index-list/history/snapshot`. Porting fedele di `championshipsHandlers.js` + `Standings.js` (fonte autorevole `standings_json` importato da LMU, ibridato con statistiche live da `race_results` via `mergeRaceStats_`, `points_adjustments_json` applicato sopra con `applyAdjustments_`, matching nome-esterno strict senza fuzzy) + `Academy.js` (PM+PP+badge percentili per-sim+paceRanking, tutto calcolato a runtime da `race_results`, nessuna tabella dedicata) + `SeasonRecap.js` + `SkillIndex.js`. **Deviazioni deliberate**: (1) `championships-add`/`championships-update` sono endpoint nuovi — il sorgente reale crea i campionati solo con funzioni Apps Script one-off eseguite dallo sviluppatore nell'editor, non praticabile per team abbonati al SaaS senza accesso a quel codice; (2) `skill-index-snapshot` è nuovo e sostituisce il trigger time-driven settimanale di Apps Script (`runSkillIndexSnapshot`) — Supabase non ha un equivalente diretto lato client, l'endpoint fa lo stesso calcolo on-demand, invocabile da staff/admin o da un futuro cron esterno; l'idempotenza-per-giorno è un unique constraint DB invece di una scansione applicativa. **Gap scoperto e chiuso durante questo dominio**: la colonna `race_number` (Races, per distinguere Race 1/Race 2 in round endurance) esisteva nel foglio reale ma era stata omessa nel porting di Races (#250) — aggiunta ora via migrazione + `races-update` (mai in `races-add`, fedele al sorgente). Gap noti rimasti: nessuna notifica Discord/audit log su import standings o aggiustamenti punti (dipendono da #256); nessuna automazione della cadenza settimanale degli snapshot Skill Index (richiederebbe pg_cron, fuori scope per un porting di endpoint esistenti). Validato end-to-end con un campionato/gara/risultati di test, poi rimosso.

**Fase 2 — RaceRSVP + RaceCrews + TrainingInsights (#253) — fatto**, validato end-to-end. Schema `020_race_rsvps_crews.sql` (`race_rsvps` con `unique(race_id, driver_id)`; `race_crews` con `unique(race_id, driver_id)`) + 6 Edge Function: `rsvp-list/set`, `race-crews-list/add/remove`, `training-insights`. Porting fedele di `RaceRSVP.js` (upsert self-only per `(race_id, driver_id)` — la scansione manuale del sorgente è sostituita dal constraint DB nativo, stesso pattern già usato altrove), `RaceCrews.js` (un pilota su una sola vettura per gara: `race-crews-add` replica esplicitamente i due controlli applicativi del sorgente — `alreadyOnThisCar`/`onAnotherCar` — per restituire lo stesso messaggio d'errore specifico, con l'unique constraint DB come rete di sicurezza sotto) e `TrainingInsights.js` (dashboard calcolata a runtime da `best_laps`: giri "di allenamento" = `practice`/`time_trial`, record squadra per pista su qualsiasi session_type, readiness-by-track con `track_id` esplicito o pista della prossima gara). **Deviazioni deliberate**: (1) `race_crews.id` è un `uuid` nativo invece del `CREWnnn` testuale generato con scan sequenziale del sorgente — l'id non viene mai referenziato altrove, quindi non serve un formato leggibile; (2) `"VSD001"` hardcoded → `drivers.is_system_account`, stesso principio già applicato in `academy-ranking`/`records-team`. Gap noto: i promemoria Discord/push per RSVP mancanti (`checkAndNotifyRsvpReminders_`) dipendono da #256, non portati qui. Validato end-to-end con una gara di test (RSVP upsert con cambio status, doppio tentativo di assegnazione — stessa vettura e vettura diversa, entrambi correttamente rifiutati con il messaggio atteso — rimozione, training.insights con dati reali), poi rimossa.

**Fase 3 — Clash of Classes (#254) — fatto**, validato end-to-end. Schema `021_clash_of_classes.sql` (`clash_participants`, `clash_results`, `clash_incident_reports`) + 9 Edge Function: `clash-participants-list/register/add/update/remove`, `clash-results-submit-round`, `clash-standings`, `clash-incidents-report/list`. Porting fedele di `ClashOfClasses.js` (mini-campionato esibizione GTE vs GT3, doppia classifica classe+assoluta, Trofeo delle Classi cap. 7/8 regolamento, vetture omologate per classe). **DEVIAZIONE ARCHITETTURALE NUOVA** (prima volta in questo progetto): a differenza di ogni altro dominio portato finora, questo è pensato dal sorgente per essere PUBBLICO — `participants.list/register`, `standings` e `incidents.report` non richiedono auth (evento community-wide, cap. 2.2 regolamento). Il sorgente reale ha un solo team implicito (VSD hardcoded); qui un chiamante anonimo non ha nessun team_id risolvibile dall'auth, quindi le Edge Function pubbliche accettano un `team_slug` (risolto su `teams.slug`) SOLO per chiamate anonime — un chiamante autenticato usa sempre il proprio team_id, ignorando team_slug. Queste stesse Edge Function operano con un client SERVICE ROLE per bypassare la RLS sulle letture/scritture pubbliche, perché il sorgente reale non ha alcun concetto di permessi riga-per-riga sullo sheet (ogni controllo è nel codice del handler, non nello storage) — la RLS in `021_clash_of_classes.sql` resta comunque attiva come barriera per accessi diretti via REST con anon/authenticated key, fedele al principio "nessuna tabella leggibile da anon direttamente" già usato ovunque in questo progetto. Altra deviazione: `driver_id` è uuid nullable (FK a drivers) invece del testo libero del sorgente — un iscritto/risultato "esterno" (community non tesserata) ha semplicemente `driver_id` null, matching per driver_id OR display_name case-insensitive fedele al sorgente. Validato end-to-end con 3 iscritti di test (uno pubblico anonimo, uno via staff-add, uno rimosso), un round di risultati con re-invio idempotente (sostituzione, non duplicazione), classifiche GTE/GT3/Assoluta/Trofeo verificate a mano contro i punteggi attesi, e una segnalazione incidenti pubblica — poi tutto rimosso.

**Fase 3 — DiscordMessenger + AuditLog + Push (#256) — fatto**, validato end-to-end. Schema `023_audit_push.sql` (`audit_log`, append-only, select ristretto a staff/admin del proprio team, insert consentito solo per il proprio `driver_id` o `null` nel proprio team; `push_subscriptions`, `unique(driver_id, endpoint)`, tutto l'accesso ristretto al proprio `driver_id`) + 4 Edge Function: `audit-log-list` (filtri `action`/`driver_id`/`q`, paginazione, arricchimento `driver_name`), `messenger-send` (porting fedele di `DiscordMessenger.js`: `mode='channel'` posta su un webhook fisso risolto da una mappa hardcoded `channel_key→env var` — mai un nome di property passato dal client, stesso principio di sicurezza del sorgente; `mode='dm'` delega al relay Vercel `discord-dm-relay` invece di chiamare Discord direttamente, perché — esattamente come nel sorgente — l'endpoint di apertura DM di Discord risponde 403 a `UrlFetchApp`/chiamate dirette dietro Cloudflare; gate a due vie `role IN (staff,admin) OR can_message`), `push-subscribe`/`push-unsubscribe` (porting fedele di `Push.js`: dedup su `(driver_id, endpoint)` con `already_existed:true` invece di riga duplicata, unsubscribe idempotente con nota esplicita se già rimossa). **Deviazione deliberata**: l'invio push reale (VAPID + crittografia `aes128gcm`) NON è stato reimplementato nativamente in Deno — a differenza di Apps Script, l'ambiente Edge Function avrebbe il Web Crypto necessario, ma la scelta è stata continuare a delegare al relay Vercel già testato in produzione (`PUSH_RELAY_URL`/`PUSH_RELAY_SECRET`) invece di duplicare un'infrastruttura funzionante — documentato nell'header dello schema. **Gap chiuso in questa fase**: tutti i 7 punti "`logAudit_`/notifica Discord omessi, deferiti a #256" aperti durante #255 sono stati retrofittati nei rispettivi Edge Function già deployati (`candidates-add`/`candidates-update`/`sponsors-add`/`sponsors-update`/`treasury-add`/`treasury-update`/`treasury-remove`), verificati fedeli riga per riga contro `Candidates.js`/`Sponsors.js`/`Treasury.js` (es. `sponsors-update` logga SOLO su cambio status, e invoca `notifySponsorActivated` in nested-if solo quando il nuovo status è `active`, esattamente come il sorgente; `candidates-remove`/`sponsors-remove` NON sono stati toccati perché il sorgente reale non chiama `logAudit_` in quei due handler — nessun gap da chiudere lì). **Gap noti rimasti, esplicitamente fuori scope per questa fase**: i gap `logAudit_`/notifiche più vecchi in Races (#250: races-add/update/remove/update-poster/update-gallery), Incidents (#251: incidents-resolve), Championships (#252: import-standings/save-adjustments) e RaceRSVP (#253: `checkAndNotifyRsvpReminders_`) NON sono stati retrofittati in questa fase — restano documentati come debito residuo, da chiudere quando si ripasserà su quei domini o in una fase di hardening dedicata; i secrets Discord (`DISCORD_WEBHOOK_ADMIN_URL`, `DISCORD_WEBHOOK_BARSPORT_URL`, `DISCORD_WEBHOOK_GESTIONE_GARE_URL`, `DISCORD_RELAY_URL`, `DISCORD_RELAY_SECRET`) non sono ancora configurati come Edge Function secrets su Supabase — verificato end-to-end: `messenger-send` risponde correttamente `webhook_not_configured` invece di crashare, e le notifiche Discord non bloccanti (`notifyNewCandidate`/`notifyNewSponsorLead`/`notifySponsorActivated`) falliscono silenziosamente come da design fault-tolerant, senza impedire la scrittura reale — la sola configurazione dei secrets (stesso principio delle Script Properties in Apps Script) attiverà le notifiche senza altre modifiche al codice. Validato end-to-end: `audit-log-list` su base vuota e con righe reali; ciclo completo candidato/sponsor/movimento cassa (add/update/remove) con verifica testuale esatta di ogni riga `audit_log` prodotta contro il formato del sorgente; `push-subscribe` con dedup (`already_existed`) e `push-unsubscribe` idempotente; `messenger-send` mode=channel con gate ruolo superato e fallimento pulito per webhook non configurato — poi tutti i dati di test rimossi (audit_log via SQL diretto, non essendoci un endpoint di cancellazione per design).

**Fase 4 — LapData / Analisi di Passo (#258) — fatto**, validato end-to-end. Schema `025_lap_data.sql` (`lap_data`, un giro per riga, import CSV dal plugin SimHub — sola scrittura, nessun update/delete esposto, fedele al sorgente; `id` uuid nativo invece del `LAP-timestamp-idx` testuale, stesso principio già applicato a `race_crews.id`) + 3 Edge Function: `lap-data-import` (porting fedele di `LapData.js`: parser CSV RFC4180-ish carattere-per-carattere identico al sorgente inclusa la gestione di campi quotati con virgole/newline interni, `matchDriverName` riusa esattamente l'algoritmo fuzzy nome+iniziale-cognome già validato in `race-results-import`, dedup `(session_id, driver_key, lap_number)` via `Set` costruito leggendo le righe esistenti della sessione — non un vincolo DB, fedele al sorgente perché `driver_key` è condizionale — driver_id se riconosciuto, altrimenti nome esterno minuscolo), `lap-data-sessions` (elenco sessioni aggregato al volo, più recenti prima), `lap-data-session` (dettaglio giri con flag `clean` = non in_pits/non yellow_flag, stessa definizione di `isCleanLap_` già in FuelLog). Nessuna colonna "valid"/LapStatus a 6 stati: la shared memory LMU non espone un'invalidazione giro per taglio pista, quindi settori/velocità/in_pits/yellow_flag restano nullable per compatibilità con CSV generati da versioni precedenti del plugin — stessa scelta del sorgente. Auth: `lapData.import` gated su staff/admin, `lapData.sessions`/`lapData.session` aperte a qualsiasi pilota autenticato del team, nessuna restrizione di ruolo — fedele al sorgente. Validato end-to-end: import di un CSV di 3 righe (2 giri di un driver riconosciuto via fuzzy-match più 1 esterno non abbinato) con conteggio `vsd_matched`/`external` corretto, un secondo import dello stesso CSV correttamente scartato integralmente come duplicato (`skipped_duplicates: 3`), `lap-data-sessions` con aggregazione corretta (1 sessione, 3 giri, 2 driver distinti), `lap-data-session` con dettaglio giri e flag `clean` verificato riga per riga (`in_pits: true` → `clean: false`, riga esterna pulita → `clean: true`) — poi tutti i dati di test rimossi via SQL diretto (nessun endpoint di cancellazione per design, fedele al sorgente sola-scrittura).

**Fase 4 — Fuel/Energy + Devices (#257) — fatto**, validato end-to-end. Schema `024_fuel_log_devices.sql` (`fuel_log`, un campione di consumo per giro, nessuna FK su `race_id` — fedele al sorgente, che non ha alcun vincolo referenziale: il companion in modalità solo genera un `race_id` locale non necessariamente presente nel calendario; `fuel_live_pings`, sostituisce lo Script Properties key-value del sorgente con una riga upsert per `(team_id, race_id, car_number)`, deviazione necessaria — Postgres non ha un equivalente diretto di una proprietà scriptwide effimera) + 6 Edge Function: `fuel-log-sample`, `fuel-log-live`, `fuel-summary`, `fuel-my-session`, `fuel-stints` (porting fedele di `FuelLog.js`: media mobile consumo/energia su finestra configurabile con esclusione dei giri "sporchi" — `in_pits`/`yellow_flag` — dalla media, ping live scartato se più vecchio di 2 minuti, raggruppamento in stint con hotstint sul miglior passo medio tra gli stint con almeno 3 giri puliti, sessione personale attiva solo se l'ultimo campione risale a meno di 30 minuti fa) e `devices-create-token` (porting fedele di `Devices.js`: token long-lived stateless per il companion app, TTL 180 giorni). **DEVIAZIONE ARCHITETTURALE NUOVA**: il sorgente autentica sia le sessioni web sia i token companion con lo stesso `verifyToken()` HMAC stateless di `discordAuth.js` — Supabase Auth invece usa JWT di sessione con TTL breve, incompatibili con un companion che deve restare autenticato per un'intera stagione senza ri-loggare via Discord ogni ora. Le 5 Edge Function `fuel-*` implementano quindi un doppio percorso di autenticazione (`resolveDriver`, duplicato in ognuna per convenzione di repo): prima tenta di verificare l'`Authorization` come device token HMAC-SHA256 stateless (payload `driver_id|team_id|expiresAtMs` + firma, secret `DEVICE_TOKEN_SECRET`, nessuna tabella, nessuna revoca individuale — stessa limitazione accettata nel sorgente), poi in fallback lo tratta come sessione Supabase normale (`auth.getUser()` + lookup `drivers`) — lo stesso Authorization header copre entrambi i casi, fedele all'idea di un unico meccanismo di verifica del sorgente. Tutte le `fuel-*`/`devices-create-token` operano con client SERVICE ROLE (mai anon+JWT) perché un chiamante via device token non ha mai una sessione Supabase reale — stesso principio già stabilito per gli endpoint pubblici di Clash of Classes/ChampionshipInterest. `verify_jwt=false` a livello di deployment per le 5 `fuel-*` (un device token non è un JWT Supabase valido, quindi il gate JWT di piattaforma andrebbe in errore su ogni chiamata companion) — l'autenticazione reale è interamente nel codice del handler. **Deviazione minore**: `team_id` è nel payload del device token in più rispetto al sorgente (single-tenant, nessun bisogno di scoping team) e non presente nel sorgente; `tier`/`sims` del sorgente sono omessi dal payload perché nessun handler `fuel.*` portato li legge mai. **Gap noto, non bloccante**: il secret `DEVICE_TOKEN_SECRET` non è ancora configurato come Edge Function secret su Supabase — verificato end-to-end: `devices-create-token` risponde correttamente `DEVICE_TOKEN_SECRET non configurato` (500) invece di crashare in modo opaco; una volta configurato il secret, il flusso device-token si attiva senza altre modifiche al codice (stesso principio dei webhook Discord di #256). Validato end-to-end via sessione Supabase normale (il companion reale non è stato testato qui, essendo un client esterno): due campioni per-giro con delta consumo/passo, un ping live, `fuel.summary` con media mobile/proiezione autonomia/aggregati velocità session-wide verificati a mano, `fuel.mySession` risolto correttamente sull'ultimo giro (ignorando il ping live, fedele al sorgente), `fuel.stints` con confine di stint su `in_pits` verificato (due stint da 1 giro ciascuno, 0 giri puliti ciascuno, nessun hotstint sotto soglia) — poi tutti i dati di test rimossi via SQL diretto (nessun endpoint di cancellazione per design, fedele al sorgente sola-scrittura).

**Fase 3 — ChampionshipInterest + PrequalCandidates + Candidates + Sponsors + Treasury + Consent (#255) — fatto**, validato end-to-end. Schema `022_admin_domains.sql` (`championship_interest`, `prequal_candidates`, `candidates`, `sponsors`, `treasury_entries`, `consents`) + 23 Edge Function su 6 sotto-domini: `interest-list/register/update/remove`, `prequal-list/add/remove` (porting fedele di `ChampionshipInterest.js`/`PrequalCandidates.js` — stesso pattern pubblico/team_slug/service-role già usato per Clash of Classes, essendo entrambi pubblici nel sorgente); `candidates-list/add/update/remove`, `sponsors-list/add/update/remove` (porting fedele di `Candidates.js`/`Sponsors.js`, staff-o-admin, validazione status contro `CANDIDATE_STATUSES`/`SPONSOR_STATUSES`, sort di `sponsors.list` per `next_follow_up` con null trattato come lontano nel futuro); `treasury-list/add/update/remove`, `consent-status/accept/admin-list/social-flags` (porting fedele di `Treasury.js`/`Consent.js`). **Deviazioni deliberate**: (1) `treasury_entries.amount` è `numeric(12,2) check (amount > 0)` — il vincolo "sempre positivo, segno derivato da `type`" del sorgente (mai un numero negativo in storage) è ora imposto dal DB, non solo dalla validazione applicativa; (2) `sponsors.next_follow_up` è colonna `date` nativa invece di stringa libera; (3) `consent.accept` fa upsert via vincolo DB `unique(team_id, driver_id, consent_version)` invece della scansione manuale delle righe del sorgente — stesso esito (un pilota può risottomettere senza duplicati), più robusto lato DB; (4) `consent.socialFlags` nel sorgente è pubblica e senza NESSUN parametro di scoping (single-tenant, un solo team implicito) — qui, essendo multi-tenant, richiede comunque `team_slug` per le chiamate anonime, stesso pattern pubblico già consolidato con Clash of Classes/ChampionshipInterest/PrequalCandidates, per evitare data leakage tra team diversi (deviazione necessaria, non presente nel sorgente). **Gate admin-only preservato fedelmente**: `treasury.*` e `consent.adminList` richiedono `current_driver_is_admin()`, non lo staff-o-admin più comune altrove — verificato end-to-end forzando temporaneamente il ruolo del driver di test a `staff` e confermando il rifiuto 403, poi ripristinato ad `admin`. **Gap noti** (tutti deferiti a #256): `logAudit_` omesso in tutte le scritture di tutti i 6 sotto-domini; notifiche Discord omesse (`notifyNewCandidate_`, `notifyNewSponsorLead_`, `notifySponsorActivated_`, `runSponsorFollowUpDigest`); il testo del consenso mostrato ai piloti resta responsabilità frontend, fuori scope per questo porting backend (non è un parere legale, vedi header `Consent.js`). Validato end-to-end con: iscrizione interesse pubblica+self+dedup+update+soft-delete, candidato prequalifica pubblico add/list/remove, candidato pipeline full CRUD con validazione status, sponsor full CRUD con validazione status e verifica ordine sort per `next_follow_up`, movimento cassa full CRUD con vincolo `amount > 0` e verifica totali/saldo, consenso accept/status/upsert-su-riaccettazione/adminList/socialFlags con calcolo `is_minor` server-side — poi tutto rimosso.

**Fase 4 — Endurance: Auditions + Participants + Stints (#259) — fatto**, validato end-to-end via Chrome reale (login Discord OAuth su `/admin/calendar-preview`, chiamate dirette alle Edge Function con la sessione risultante). Schema `026_endurance.sql` (`endurance_auditions`, `endurance_participants` con `unique(audition_id, driver_id)` e FK a `endurance_auditions`/`drivers`, `endurance_stints` con `unique(team_id, race_id, car_number, stint_order)`). Porting fedele di `Endurance.js` + `EnduranceParticipants.js` + `EnduranceStints.js`: il quarto sotto-dominio storico `EnduranceAuditionStints` risulta orfano/mai referenziato (commento esplicito in `Codice.js`, "rimossa (audit ago 2026): mai referenziata") ed è stato correttamente escluso dallo scope. Gate di ruolo preservati fedelmente per-azione, non genericamente "staff": `auditions.create/update` → staff-o-admin; `participants.add/update/remove` e TUTTE le azioni `stints.*` di scrittura → SOLO admin, nonostante i nomi delle funzioni sorgente (`_epIsAdmin_`, `_esIsStaff_`) suggeriscano diversamente — verificato leggendo il corpo reale di quelle funzioni, non i loro nomi. `stints.list` è stato portato come sessione autenticata realmente obbligatoria (`verify_jwt` + `auth.getUser()`), più severo del sorgente reale (dove `doPost` costruisce comunque un ctx anonimo, rendendo il check `Auth: richiesta` del codice sorgente dead code in produzione) — deviazione deliberata verso il comportamento *inteso* dal commento originale, non verso il comportamento *effettivo* del sorgente.

**DEVIAZIONE ARCHITETTURALE NUOVA (infrastruttura, non logica)**: a metà del deploy di questo dominio il progetto Supabase ha raggiunto il tetto di 100 Edge Function del piano free (`PaymentRequiredException` al 4° deploy). L'API Supabase MCP non espone un `delete_edge_function`, quindi gli slug già consumati non sono recuperabili. L'utente ha scelto esplicitamente di restare gratis ("ottimizza per non cambiare piano e mantenere tutto gratuito", 17 set 2026) invece di alzare il piano o disattivare lo spend cap. Soluzione: le 15 azioni logiche del dominio (4 lettura + 11 scrittura) sono state consolidate in DUE Edge Function dispatch-by-`payload.action`, deployate RIUTILIZZANDO due slug già consumati (redeploy, nuova versione) invece di crearne di nuovi — zero slot aggiuntivi consumati, zero funzionalità perse, nessuna logica riscritta rispetto a cosa sarebbe stato ciascun endpoint singolo:
- **`endurance-read`** (deployata sotto lo slug `endurance-auditions-list`, v2) — `auditions.list/get`, `participants.list`, `stints.list`.
- **`endurance-write`** (deployata sotto lo slug `endurance-auditions-create`, v2) — le 11 azioni di scrittura, sessione/driver risolti una sola volta in testa a `Deno.serve`, poi dispatch su `payload.action` con check di ruolo per-azione.
- Lo slug gemello `endurance-auditions-get` (deployato prima della decisione di consolidare) resta come mirror legacy ridondante di `auditions.get` — inoffensivo, occupa comunque uno slot già consumato.
- Verificato per via statica con `get_edge_function`: il codice effettivamente deployato sui due slug coincide byte-per-byte con `cloud/functions/endurance-read/index.ts`/`endurance-write/index.ts` nel repo. Tutti gli altri file locali `cloud/functions/endurance-*` (le versioni single-action mai deployate) sono stati sovrascritti con uno stub di 5 righe che punta al dispatcher canonico, per non fuorviare chi legge il repo.
- **Precedente per #260-#265**: da qui in avanti, per ogni nuovo dominio, valutare fin dall'inizio un dispatcher consolidato per lettura/scrittura invece di una function per azione, per non ririschiare il tetto.

Altre deviazioni: `shiftStintsOrder` esegue UPDATE riga-per-riga in ordine DESC (delta>0) o ASC (delta<0) per rispettare il vincolo UNIQUE `(team_id, race_id, car_number, stint_order)` senza deferred constraints (non supportati via REST); `parseNaiveAsUtcMs`/`formatUtcAsNaiveIso` trattano gli ISO naive come UTC invece di usare il timezone locale del runtime (Deno Edge Function non ha un "locale" controllabile, a differenza di Apps Script) — applicato coerentemente in `stints.generate` e `stints.validateCoverage`; vincoli DB reali (FK, UNIQUE) sostituiscono i controlli manuali di esistenza/duplicato del sorgente per `endurance_participants`, stesso principio già usato per `consents`.

**Bug trovato e corretto durante la validazione live**: `stints.update` con riordino (`stint_order` diverso dal valore esistente) falliva con `duplicate key value violates unique constraint` quando lo spostamento coinvolgeva più righe intermedie. Causa: la riga che si sta spostando resta fisicamente al suo `stint_order` vecchio fino all'UPDATE finale della funzione, quindi lo shift delle righe intermedie (`shiftStintsOrder`) può provare a scrivere esattamente quel valore prima che venga liberato — collisione transitoria sul vincolo UNIQUE. Il sorgente Apps Script non soffre di questo problema (righe di uno Sheet, nessun vincolo UNIQUE): è una conseguenza diretta dell'aver sostituito il controllo applicativo con un vincolo DB reale, non presente nella logica originale. **Fix**: prima di eseguire i due shift, la riga viene parcheggiata su un valore sentinella (`999999`, fuori dall'intervallo realisticamente in uso ma comunque `>=1` per rispettare il CHECK `endurance_stints_stint_order_check`) così nessuno shift intermedio la trova più al vecchio posto; l'UPDATE finale la riporta al valore reale. Redeployato su `endurance-auditions-create` (v3) e riverificato con successo (spostamento di uno stint dalla posizione 4 alla 1 con 3 righe intermedie, riordino corretto). File canonico aggiornato: `cloud/functions/endurance-write/index.ts`.

**Validato end-to-end** (sessione admin reale, driver VSD005, team `vsd`): `auditions.create` (calcolo `duration_minutes_ingame`/`end_time_ingame` corretto), `auditions.get`, `auditions.update`, `auditions.list` (isStaff risolto correttamente, masking `notes_internal` per non-staff non ritestato direttamente ma logica invariata da `sanitizeAudition`); `participants.add` (con rifiuto duplicato `unique(audition_id, driver_id)` verificato), `participants.list`, `participants.update` (incl. rifiuto status non valido); `stints.generate` (round-robin autisti, orari naive UTC corretti, somma durata verificata), `stints.add` con `shiftStintsOrder` (inserimento a metà classifica, righe successive shiftate senza collisioni), `stints.list` (auth realmente richiesta, ordinamento per `car_number`+`stint_order`), `stints.update` con riordino (bug trovato e corretto, poi riverificato), `stints.validateCoverage` (rilevati correttamente un `overlap` di 5 min e due `unbalanced_share` al ±28%), `stints.confirmPlan` sia con `replace_existing:false` (rifiutato correttamente in presenza di stint esistenti) sia `true` (sostituzione pulita), `stints.remove` (chiusura del buco lasciato in classifica verificata). Dati di test rimossi: stint via `stints.remove`, partecipanti via `participants.remove`, audition di test via `DELETE` diretto (nessun `auditions.remove` esposto, fedele al sorgente). Tabelle verificate vuote a fine sessione.

**Dominio Incidents (#251, parte 2) — fatto**, validato end-to-end. Schema `017_incidents.sql` (`incident_reports` + `incident_resolutions`) + 3 Edge Function (incidents-report, incidents-list, incidents-resolve). **Deviazione architetturale esplicita, decisa con l'utente**: il sorgente reale riceve le segnalazioni da un Google Form esterno letto in sola lettura — non riproducibile da un Edge Function senza un Service Account Google dedicato. L'utente ha scelto di sostituire l'intake con un modulo di segnalazione nativo su Supabase (`incidents.report`, nuovo, non esiste nel sorgente): i piloti segnalano direttamente autenticati, niente più matching nome-esterno per il segnalante. `incidents.list`/`incidents.resolve` restano fedeli alla logica reale (visibilità ristretta ai piloti coinvolti, staff_notes nascosto ai non-staff, upsert per formalizzazione). Gap noto: nessuna notifica Discord/audit log post-resolve (#256).

**Dominio RaceResults (#251, parte 1) — fatto**, validato end-to-end. Schema `016_race_results.sql` (`race_results`, FK a `races.race_id`) + 2 Edge Function (race-results-list, race-results-import). Porting fedele di `RaceResults.js` + `RaceResultsImport.js`: matching nome-esterno→driver_id multi-livello (`matchDriverName_`), dedup per (race_id, session_type, driver_key), doppio formato LMU/iRacing con normalizzazione sessioni multiple. Deviazione deliberata: `race-results-list` richiede auth (il sorgente reale è pubblico/anonimo — non replicabile in modo pulito nel multi-tenant senza un parametro di scoping team aggiuntivo, fuori scope qui). Gap noto rimasto: nessuna notifica Discord post-import (dipende da #256, non ancora portato). Il gap "nessun seeding Race Reports" è stato **chiuso in #261**: `race-results-import` ora chiama `seedRaceReportsForRace()` in modo non bloccante dopo ogni import con `session_type==='race'` — vedi nota #261 sotto.

**Dominio Races (#250) — fatto**, validato end-to-end. Schema `015_races.sql` (`races`, race_id testuale `RACEnnn` come PK condivisa tra team, nuovo helper RLS `current_driver_is_admin()`) + 8 Edge Function (races-list, races-upcoming, races-get, races-add, races-update, races-remove, races-update-poster, races-update-gallery) deployate. Porting fedele di `apps-script/Races.js`, incluso il gate a due livelli reale (races.add/update/remove SOLO admin, races.updatePoster/updateGallery staff-o-admin) e la normalizzazione URL Google Drive. Gap noti e documentati nel codice: `championship_name` sempre null (dominio Championships non portato, #252), nessun controllo "stint collegati" su races.remove (dominio Endurance non portato, #259), nessun `logAudit_` (dominio AuditLog non portato, #256). Deviazioni deliberate: campi opzionali assenti scritti come `null` invece di `''`, `gallery_urls` è `text[]` nativo invece di CSV in una cella, generazione `race_id` con scan globale (client service-role) per garantire unicità della PK condivisa tra team. Rimossa in questa fase anche una tabella `races` orfana pre-esistente nel progetto Supabase (schema demo non tracciato da nessuna migrazione — `pilots/seasons/races/race_results/penalties`, vuota, confermata rimozione con l'utente) — vedi `014_drop_orphan_demo_schema.sql`.

**Migrazione dati storici (#245) — completata.** Dati reali del foglio `VSD_HUB_DB` migrati in Supabase (drivers già erano allineati, nessun re-sync necessario): `cars` 163 righe, `tracks` 194 righe (schema esteso con `manufacturer`/`category`/`variant`/`length_km`/`country`/`garage61_id` per rispecchiare i campi reali; duplicato `lmu-spa-gp` disambiguato in `lmu-spa-gp-endurance`, vedi #125), `best_laps` 461 righe (`car_id` reso nullable per righe reali senza auto associata; alcuni timestamp del foglio sorgente erano malformati — es. `T108:02.000Z` — normalizzati durante l'import), `team_sessions` 1 riga (allenamento collettivo Barcellona) + `session_rsvps` 2 righe. Questa è comunque solo una migrazione dei **dati**, non un cutover: il sito pubblico reale continua a girare su Apps Script finché non viene deciso esplicitamente di spostare il backend live su Supabase.

**Fase 5 — Social Manager (#260) — fatto**, validato end-to-end via Chrome reale (login Discord OAuth su `/admin/calendar-preview`, chiamate dirette alla Edge Function con la sessione risultante, sessione admin reale demetrio_crucitti/team `vsd`). Schema `027_social_manager.sql` (`social_posts`, `social_metrics`, `social_media`, `social_plan_dismissed`). Porting fedele di `apps-script/SocialManager.js`: CRUD post con transizione bozza→programmato→pubblicato e `published_at` timbrato solo al primo passaggio a pubblicato, tracking follower manuale multi-piattaforma, generazione testo AI dual-provider (Anthropic/Gemini, stesso system prompt, stessi modelli), statistiche membri Discord via relay Vercel (`discord-invite-stats`), metadati Media Gallery (i file restano su Vercel Blob, mai toccati da qui), archiviazione piano editoriale con chiave `(race_id, pillar)` dove pillar vuoto = whole-race e pillar valorizzato = singola azione (retrocompatibile). **Limite strutturale preservato**: nessuna pubblicazione automatica su alcun social — "pubblicato" resta un marcatore manuale, nessun connettore Facebook/Instagram costruito.

**DEVIAZIONE ARCHITETTURALE — NUOVA per questa fase, riguarda l'intero progetto**: il tetto di 100 Edge Function del piano free (già documentato in #259) significa che *ogni* dominio da qui in avanti deve nascere già consolidato, non solo Endurance. A differenza di Endurance (due dispatcher, `endurance-read`/`endurance-write`, perché lì i reads erano in parte pubblici e i writes no), Social Manager ha un'unica policy d'accesso per tutte le 15 azioni (SOLO admin, mai staff — fedele a `ctx.isAdmin` nel sorgente), quindi non c'è alcun bisogno funzionale di separare lettura/scrittura: **un solo dispatcher** (`social-manager`, sessione/driver risolti una volta in testa a `Deno.serve`, poi `switch` su `payload.action`) copre tutto. Deployato RIUTILIZZANDO lo slug `endurance-auditions-get` (redeploy, nuova versione) — quello slug era già documentato come mirror legacy ridondante di `auditions.get`, superato da `endurance-read` fin da subito, quindi libero da riassegnare senza perdere alcuna funzionalità reale. Zero slot aggiuntivi consumati, tetto ancora a 100/100. File locale canonico: `cloud/functions/social-manager/index.ts` — `cloud/functions/endurance-auditions-get/index.ts` sovrascritto con uno stub che rimanda lì, stesso trattamento già dato ai file single-action orfani di Endurance. **Precedente confermato per #261-265**: continuare a valutare un dispatcher unico (o al massimo read/write separati solo se l'auth lo richiede davvero) prima di aprire nuovi slug.

Azione nuova non presente nel sorgente: `plan.runDigest`, equivalente on-demand di `runSocialPlanDigest` (il trigger settimanale Apps Script non ha un corrispettivo lato client Supabase — stesso principio già usato per `skill-index-snapshot` in #252). Replica 1:1 la logica di bucket/pillar del sorgente, inclusa una particolarità non corretta deliberatamente: il sorgente considera QUALSIASI riga di `SocialPlanDismissed` per una gara (anche con `pillar` valorizzato) come "salta tutta la gara" nel calcolo del digest, mentre `useEditorialPlan` lato frontend filtra correttamente solo le righe whole-race — riprodotto fedele al comportamento reale del trigger, non a quello inteso. **Gap noto, non bloccante**: nessuno dei secrets richiesti (`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, `DISCORD_INVITE_CODE`, `DISCORD_INVITE_RELAY_URL`, `DISCORD_RELAY_SECRET`, `DISCORD_WEBHOOK_GESTIONE_GARE_URL`) è ancora configurato come Edge Function secret su Supabase — verificato end-to-end che `generateText`/`discord.stats`/`plan.runDigest` rispondono con errori controllati (400, messaggio esplicito) invece di crashare; una volta configurati (`DISCORD_WEBHOOK_GESTIONE_GARE_URL` è già lo stesso secret di `messenger-send`/#256, quindi potrebbe essere già attivo lì) i flussi si attivano senza altre modifiche al codice. Nessun `logAudit_`/audit trail: il sorgente reale non chiama mai `logAudit_` in questo dominio, quindi nessun gap da chiudere (a differenza di Races/Candidates/Sponsors/Treasury). Nessuna automazione della cadenza settimanale del digest (richiederebbe pg_cron, fuori scope per un porting di endpoint esistenti — stesso principio già annotato per Skill Index).

Validato end-to-end: CRUD post completo (create con validazione content/platforms, update con transizione a pubblicato e `published_at` corretto, list con filtro status, remove con 404 su id inesistente o mal formato), tracking metriche (add + list ordinata, nessun vincolo di unicità per data — fedele), Media Gallery (add con inferenza `media_type` da estensione, list con filtro tag case-insensitive su stringa CSV, remove), piano editoriale (`plan.dismiss` whole-race e per-singola-azione con upsert idempotente — flag `already` verificato in entrambi i casi —, `plan.undismiss` con 404 corretto su sezione non archiviata, `plan.dismissedList`), `plan.runDigest` con una gara di test reale (bucket "questa settimana"/evergreen calcolati correttamente, gara dismissed correttamente esclusa, webhook non configurato gestito senza crash), `generateText`/`discord.stats` con errori controllati per secrets assenti, gate SOLO-admin verificato forzando temporaneamente il ruolo del driver di test a `staff` (403 confermato) e ripristinato ad `admin`. Dati di test rimossi via SQL diretto (nessun endpoint di cancellazione per `metrics`/`plan_dismissed`, fedele al sorgente — solo `posts.remove`/`media.remove` esistono davvero).

**Fase 6 — Race Reports + Reazioni + aggregatori Showcase/landing.data (#261) — fatto**, validato end-to-end via Chrome reale (login Discord OAuth su `/admin/calendar-preview`, chiamate dirette alla Edge Function con la sessione risultante, sessione admin reale demetrio_crucitti/team `vsd`). Schema `028_race_reports.sql` (`race_reports` con `unique(team_id, race_id, driver_id)`, `report_reactions` con `unique(report_id, driver_id)` e check sul set fisso di 5 emoji). Porting fedele di `apps-script/Reports.js` (privacy aggiornata 20/08/2026 nel sorgente: report visibili a tutto il team loggato, non solo all'autore — prerequisito per le reazioni tra piloti; `staff_rating`/`staff_notes` sempre restituiti, mascherati solo lato frontend), `Showcase.js` (due aggregatori pubblici `showcase.summary`/`showcase.mediaKit` per le pagine `/joinus` e media-kit: podi unione+dedup da `race_reports`+`race_results`, giri verificati, top piloti, prossime gare, ultimo giro veloce, breakdown sim, anno di fondazione, metriche social ultimo-per-piattaforma), `LandingData.js` (`landing.data`, un aggregatore che combina 9 fonti in una sola chiamata — stesso identico contenuto di 9 chiamate separate, replicato inline invece che con 9 fetch interni), `seedReports.js` (`seedRaceReportsForRace_`, seeding automatico bozze report post-import). **Scope anticipato da #262**: `laps.raceLaps` (`apps-script/BestLaps.js`, `handleLapsRaceLaps`) è stato portato QUI invece che in #262, perché `landing.data` ne ha una dipendenza diretta e la logica è autosufficiente (giri derivati 1:1 da `race_results`/`races`/`cars` già portati, nessuna tabella nuova, nessuna dipendenza da `syncFromGarage61`/Best Lap Submissions che restano l'unico scope reale di #262). Matching nome-auto→car_id (`buildCarNameToIdMap_`) verificato contro le colonne reali di `cars` (`car_name`, `full_name` — le colonne `name`/`display_name` controllate dal sorgente non esistono su questo schema, quindi omesse). **Deviazioni deliberate, non presenti nel sorgente**: (1) `reports.update`, endpoint nuovo — il sorgente compila `incident_notes`/`damage_report`/`strategy_notes`/`staff_rating`/`staff_notes` solo a mano nel Google Sheet, non praticabile per un team abbonato al SaaS senza accesso a quel foglio, stesso principio già usato per `championships-add`/`update` (#252), gate staff-o-admin; (2) `reports.seedForRace`, endpoint nuovo — equivalente invocabile di `seedRaceReportsForRace_` (il sorgente lo esegue sempre automaticamente post-import, mai come azione a sé), utile per backfillare gare importate prima di questo porting, gate staff-o-admin. **Gap chiuso in questa fase**: il gap "nessun seeding Race Reports" documentato in #251 è stato chiuso retrofittando `race-results-import/index.ts` con `seedRaceReportsForRace()`, chiamata in modo non bloccante (try/catch fault-tolerant, mai propagato al chiamante) dopo ogni `importGroup` con `session_type==='race'`, sia per LMU sia per iRacing (fino a 3 sessioni) — fedele a `RaceResultsImport.js` righe 580-659.

**Consolidamento dispatcher, continuazione della decisione presa in #260**: nessuno slot Edge Function nuovo consumato (tetto ancora fermo a 100/100). Le azioni di questa fase sono state aggiunte come branch aggiuntivi nello stesso file/slug già usato per Social Manager (`cloud/functions/social-manager/index.ts`, deployato sotto lo slug `endurance-auditions-get`). A differenza di Social Manager (un'unica policy d'accesso, SOLO admin), questo dominio introduce **tre tier di auth diversi nello stesso dispatcher**: Social Manager resta SOLO admin, Race Reports/Reazioni/`landing.data`/`laps.raceLaps` richiedono un pilota autenticato qualsiasi, Showcase è interamente pubblico. La risoluzione dell'auth in testa a `Deno.serve` è stata quindi resa OPZIONALE (prima era un gate unico obbligatorio) — ogni gruppo di azioni applica il proprio gate (`requireAdmin()`/`requireAuth()`/`requireStaffOrAdmin()`) subito dopo aver individuato l'azione, non prima. Showcase riusa lo stesso pattern pubblico `team_slug` + client service role già consolidato con Clash of Classes/ChampionshipInterest per i chiamanti anonimi; un chiamante autenticato usa comunque service role per l'accesso cross-tabella, stesso principio. **Precedente confermato con l'utente per #262-265**: continuare a riusare/accorpare nello stesso slug (o in un futuro slug altrettanto riusato) invece di alzare piano o liberare uno slug di un altro dominio — nessun dato o funzionalità viene mai perso in questo accorpamento, solo il dispatcher fisico cresce.

Validato end-to-end con una gara di test (1 risultato VSD, posizione 1, giro 1:35.000, vettura "Ferrari 296 GT3"): `reports.seedForRace` (1 bozza generata, contatori skip corretti), `reports.list`/`reports.recent`, `reports.update` (staff_rating/staff_notes aggiornati), `reportReactions.toggle` (aggiunta 🔥 → toggle-off sulla stessa emoji → sostituzione con 👏, tutti i tre stati verificati), `landing.data` (tutte le 11 chiavi presenti, `race_laps` con matching auto corretto), `laps.raceLaps` standalone (stesso risultato di `landing.data.race_laps`), `showcase.summary`/`showcase.mediaKit` sia autenticato sia anonimo con `team_slug=vsd` (stats/topDrivers/latestBestLap/sims/social corretti), rifiuto 400 su chiamata anonima senza `team_slug`, gate 401/403 verificati su `reports.list`/`landing.data`/`reports.update` senza sessione. Dati di test rimossi via SQL diretto (nessun endpoint di cancellazione per `race_reports`/`report_reactions` lato pilota — solo staff/admin scrivono, fedele al sorgente che non espone alcuna action di scrittura ai piloti sui report).

**Fase 7 — syncFromGarage61 + Best Lap Submissions (#262) — fatto**, validato end-to-end via Chrome reale (login Discord OAuth su `/admin/calendar-preview`, chiamate dirette alla Edge Function con la sessione risultante, sessione admin reale demetrio_crucitti/team `vsd`). Schema `029_best_lap_submissions.sql` (`best_lap_submissions`, invio autonomo pilota con foto/video di prova via `evidence_url`, nessuna FK su `track_id`/`car_id` — stesso principio best-effort di `best_laps`; colonne `garage61_team_slug`/`garage61_last_sync_at` aggiunte a `teams`). Porting fedele di `apps-script/BestLaps.js` righe 560-886 (`lapSubmissions.submit/listMine/listPending/approve/reject/remove`) + `apps-script/SetupBestLapSubmissions.js` (schema colonne) + `apps-script/garage61.js` (`handleLapsSyncFromGarage61`/`garage61SyncLaps_`, unica action Garage61 esposta via API — tutto il resto del file sono script di manutenzione solo-editor, esclusi dallo scope). **Scope ridotto rispetto alla pianificazione iniziale**: `laps.raceLaps` non fa più parte di questa fase, essendo stato anticipato in #261 (vedi nota sopra). **Deviazione architetturale documentata nello schema**: `GARAGE61_TOKEN` (il vero segreto) resta un Edge Function secret GLOBALE anziché per-team — finché VSD è il solo team reale, un secret storage per-team cifrato sarebbe complessità anticipata; solo i dati non sensibili (`garage61_team_slug`, `garage61_last_sync_at`) sono colonne per-team su `teams`. Stessa scelta già fatta per `DEVICE_TOKEN_SECRET`/i webhook Discord. **Gap chiuso in questa fase**: il gap #179 ("controllo nuovo record di squadra + notifica Discord non portato in `laps.add`/`best-laps-add`") è stato chiuso — nuovo helper condiviso `addBestLapWithRecordCheck` (porting fedele di `handleLapsAdd`) usato sia da `lapSubmissions.approve` sia, duplicato per convenzione di repo essendo uno slug separato, in `best-laps-add/index.ts` (redeploy v2). **Nessuno slot Edge Function nuovo consumato** (tetto ancora fermo a 100/100): tutte le nuove azioni sono branch aggiuntivi nello stesso dispatcher già usato per Social Manager/#261 (`cloud/functions/social-manager/index.ts`, slug `endurance-auditions-get`, v6). Gate fedeli al sorgente: `lapSubmissions.submit/listMine` → pilota autenticato qualsiasi; `lapSubmissions.listPending/approve/reject/remove` → SOLO admin (fedele a `ctx.isAdmin`, non staff — la validazione foto/video resta riservata); `laps.syncFromGarage61` → staff-o-admin. **Gap noto, non bloccante**: il trigger a tempo ogni 4h del sorgente (`runGarage61Sync` via Apps Script trigger) non ha equivalente qui — richiederebbe uno scheduler esterno/pg_cron, fuori scope per un porting di endpoint esistenti (stesso principio già annotato per `skill-index-snapshot`/`plan.runDigest`); l'azione resta invocabile solo on-demand, come già oggi via `/admin/sync-garage61`. Il secret `GARAGE61_TOKEN` e la colonna `teams.garage61_team_slug` non sono ancora configurati per nessun team — verificato end-to-end che `laps.syncFromGarage61` risponde con l'errore controllato `garage61_team_slug non configurato per questo team` invece di crashare; una volta configurati, il flusso si attiva senza altre modifiche al codice.

Validato end-to-end: `lapSubmissions.submit` con parsing `parseLapTimeToMs` verificato su formato standard (`1:30.000` → 90000ms, `1:50.000` → 110000ms, `1:55.000` → 115000ms); `lapSubmissions.listPending` con le 3 submission di test visibili; `lapSubmissions.approve` su una submission con tempo più veloce del record di squadra esistente su quella pista (1:30.000 contro il precedente 1:32.119) — copia corretta in `best_laps` con tutti i campi, `isNewRecord` rilevato correttamente; doppio-approve sulla stessa submission correttamente rifiutato (`Richiesta già revisionata`); `lapSubmissions.reject` con `review_note`; `lapSubmissions.remove`; gate 401 verificati senza sessione su `lapSubmissions.approve`/`listPending`, 401 su `laps.syncFromGarage61` senza sessione, 400 controllato su `laps.syncFromGarage61` con team senza `garage61_team_slug` configurato. Dati di test rimossi via SQL diretto (`best_laps`/`best_lap_submissions`, nessun endpoint di cancellazione per il pilota su queste tabelle oltre a `lapSubmissions.remove`, già testato separatamente).

**Fase 7 — Pit Wall: relay Realtime per viewer remoti (#263) — fatto**, validato end-to-end via Chrome reale (login Discord OAuth su `/admin/calendar-preview`, chiamate dirette alla Edge Function con la sessione risultante, sessione admin reale demetrio_crucitti/team `vsd`). Come annotato in `010_pitwall_sessions.sql` ("FUORI SCOPE"), il sistema reale ha un bridge C# locale (`VsdPitwallBridge`) con un proprio server WebSocket `ws://localhost:8090/ws/`, raggiungibile solo da un browser sullo stesso PC — nessun viewer remoto possibile. Questa fase porta **solo l'infrastruttura di relay backend**: schema `030_pitwall_realtime.sql` (nessuna tabella applicativa — i frame live sono effimeri per definizione, mai persistiti, stesso principio del bridge originale — solo una policy RLS `select` su `realtime.messages`, la tabella di sistema che Supabase Realtime usa per autorizzare i canali privati, che concede a un pilota autenticato la lettura del solo canale broadcast `pitwall:{team_id}` del proprio team via `current_driver_team_id()` + `realtime.topic()`) e la nuova azione `pitwall.broadcastLive` (staff-o-admin, stesso gate di `pitwall.logSession`) nello stesso dispatcher `social-manager`/slug `endurance-auditions-get` (**nessuno slot Edge Function nuovo consumato**, v7). L'azione inoltra `payload.data` così com'è (schema telemetria non fissato qui, resta responsabilità del bridge/frontend in #264) su Supabase Realtime Broadcast via REST (`POST /realtime/v1/api/broadcast`, `SERVICE_ROLE_KEY` lato server) — scelta di architettura preferita a Postgres Changes perché non esiste alcuna tabella da cui derivare i cambi riga-per-riga, e Broadcast è la feature esplicitamente raccomandata da Supabase per payload ad alta frequenza tipo cursor-tracking (identico al nostro caso: posizione/tempi di ogni pilota in griglia, decine di update al secondo). Il cambio lato bridge C#/frontend che consuma davvero questo canale (sostituendo il WebSocket locale) resta scope di #264. **Nota tecnica emersa in validazione, rilevante per #264**: un client `supabase-js` creato con la sola `anon`/`publishable` key e poi con `realtime.setAuth(token)` chiamato a parte si disconnette dal canale privato pochi istanti dopo la subscribe (il client tenta un resync interno del token realtime usando la sessione — vuota — del proprio `GoTrueClient`, sovrascrivendo il token iniettato a mano) — il pattern che funziona in modo stabile è passare il token utente direttamente in `createClient(url, key, { accessToken: async () => token })`, che è anche l'API supportata da Supabase per auth di terze parti; da riusare in `usePitwallBridge` quando verrà riscritto in #264.

Validato end-to-end: sottoscrizione al canale privato `pitwall:{team_id}` con la sessione admin reale, chiamata a `pitwall.broadcastLive` con payload di telemetria sintetico, ricezione confermata lato subscriber (payload identico a quello inviato); gate 401 verificato su `pitwall.broadcastLive` senza sessione (`Operazione riservata a staff/admin`); confine RLS verificato tentando la sottoscrizione a un topic arbitrario/non proprio (`pitwall:test:...`) — correttamente rifiutata con `Unauthorized: You do not have permissions to read from this Channel topic`, a conferma che un pilota non può mai leggere il canale di un altro team. Nessun dato persistito da ripulire (nessuna tabella applicativa in questa fase).

**Fase 8 — Rewire frontend reale su Supabase (#264) — in corso.** A differenza di tutte le fasi precedenti (solo backend), questa riscrive il layer di trasporto del frontend reale (`src/api/realApi.js`, unico punto che oggi parla con Apps Script) per chiamare le ~90 Edge Function costruite in #250-263, sostituisce l'auth a token custom con Supabase Auth/Discord OAuth reale, e riscrive il bridge C# Pit Wall per pubblicare telemetria live su `pitwall.broadcastLive` invece del WebSocket locale. Approccio scelto con l'utente: **staged, per dominio** — costruire il nuovo transport layer e l'auth reale tenendoli spenti/non collegati a `client.js`, poi attivarli un dominio alla volta (pubblico→roster→calendario→best laps→...→pit wall→resto), validando via Chrome ad ogni passo prima di andare avanti — non un cutover big-bang. Il "mobile-first" richiesto per questa fase riguarda SOLO la nuova vista live remota del Pit Wall (#339), non un redesign del resto del sito.

Task #326 (routing) — **fatto**, vedi `cloud/frontend-cutover-routing-table.md`. **Errore trovato e corretto durante #328**: la prima stesura della tabella inferiva i nomi azione frontend per simmetria col nome dello slug Edge Function invece di leggere davvero `client.js` — sbagliato in più punti (es. il frontend chiama `laps.*`, mai `bestLaps.*`; `pitwall.sessions`/`pitwall.session`, non `pitwall.sessions.list`/`pitwall.session.get`; `recap.mine`, non `season.recap`; il dominio Social Manager usa il prefisso `social.` con UN'eccezione di rename esplicito — `social.plan.dismissed.list` → `plan.dismissedList` — mentre i domini aggiunti allo stesso slug in fasi successive, #261/#262/#263, sono chiamati senza alcun prefisso). Corretto leggendo per intero `client.js` + `realApi.js` (1512 righe) + il sorgente deployato del dispatcher social-manager via `get_edge_function`.

Task #327 (auth reale Discord OAuth via Supabase) — **fatto, validato end-to-end via Chrome reale**: `src/api/supabaseClient.js` (client supabase-js condiviso, prima dipendenza reale del pacchetto — le pagine di preview #177/#178 usavano fetch manuale perché il pacchetto non era ancora installato), `src/api/supabaseAuth.js` (signInWithDiscord/signOut/getSession/onAuthStateChange/resolveDriverAndTier — mappatura tier documentata inline: nessuna sessione→anonymous, sessione senza riga `drivers`→guest, `role='driver'`→pilot_vsd, `staff`→staff, `admin`→admin), pagina di validazione `/admin/auth-preview` (`AdminAuthPreview.jsx`, stesso isolamento delle preview roster/calendario). Validato in produzione (`vsd-paddock.vercel.app/admin/auth-preview`, deploy Vercel da commit `e844be4`): login Discord OAuth reale con l'app dedicata "VSD Paddock Cloud Preview" (#244), redirect PKCE completo, sessione rilevata automaticamente via `detectSessionInUrl`, profilo Discord (`discordUsername`/`discordAvatarUrl`) estratto correttamente da `user_metadata`, driver risolto correttamente (`VSD005`, demetrio_crucitti) con `tier: admin` — mappatura ruolo→tier confermata corretta sull'account admin reale del team.

Task #328 (transport layer `src/api/supabaseApi.js`) — **scritto e de-riskato staticamente, validazione dinamica per-dominio deferita a #329+**: `callApi(action, payload)` drop-in-compatibile con `realApi.js`, routing completo su tutte le azioni note, gestione dei 3 dispatcher consolidati (strip prefisso `endurance.`/`social.` con l'eccezione di rename sopra), mappa `UNWRAP_KEY` per i ~20 casi dove `realApi.js` estrae un campo da `res.data` invece di restituirlo intero, replica fedele del filtro client-side di `roster.list`/`laps.list`. Ancora NON collegato a `client.js` (per design — il cutover reale è #329+). Build di produzione verificata (`npm run build` pulito dopo l'installazione esplicita di `@supabase/supabase-js`), committato e pushato (`e844be4`, `main`).

**Blocco infrastrutturale della sessione, risolto by-pass**: la shell sandbox di Claude in questa sessione è rimasta wedged per tutta la durata (stesso errore di mount legato all'update Windows dell'8 settembre) — non risolvibile lato Claude. Aggirato facendo eseguire all'utente in locale (VS Code, macchina Windows non affetta dal bug) l'intera sequenza `npm install @supabase/supabase-js` → `npm run build` → `git add`/`commit`/`push`. Nel percorso è stato individuato e correttamente escluso dal commit un cambiamento non correlato e pre-esistente (`src/pages/UE144.jsx`, un `id="proteste"` aggiunto a una sezione, lasciato all'utente da gestire separatamente) e riconfermato — non chiuso — il debito noto `cloud/functions`/`cloud/schema` mai committati in git nonostante deployati (vedi #94).

**De-risking statico aggiuntivo di #328** (senza shell, usando solo il tool Supabase per leggere sorgenti deployati): oltre alle verifiche già fatte per roster/team-sessions/social-manager, sono stati letti byte-per-byte anche `lookups-tracks/cars`, `races-list/upcoming/get`, `championships-list`, `best-laps-list/leaderboard` — ogni singola azione della mappa `UNWRAP_KEY` di `supabaseApi.js` e i due casi con filtro client-side (`roster.list`, `laps.list`) sono ora confermati contro il codice reale, non solo dedotti. Pattern di risposta `{ok, data:{...}}` confermato identico su tutte le ~14 Edge Function lette finora in questa fase. Le restanti ~110 azioni (pass-through puro, senza unwrap né logica client-side, in `realApi.js` sono letteralmente `return await postToBackend(action, payload, token)`) hanno rischio residuo basso perché `supabaseApi.js` le tratta già con lo stesso pass-through generico — resta comunque da validare via Chrome dominio per dominio in #329-340, non dato per definitivamente assodato senza un test reale.

Gap segnalato: `presence.heartbeat`/`presence.online` non hanno alcun equivalente Supabase — decisione (droppare o ricostruire su Realtime Presence) da prendere prima di chiudere #264.

**Task #329 — Cutover Pubblico + Roster — in corso.** Prima del cutover vero e proprio, la pianificazione iniziale ("landing.data parziale pubblico") si è rivelata sbagliata: `landing.data` richiede SEMPRE `requireAuth()` lato dispatcher (verificato leggendo `social-manager/index.ts`) ed è comunque chiamata dal frontend solo quando esiste un `driverId` (`useLandingData.js`, `enabled: Boolean(driverId)`) — non ha alcun percorso anonimo, quindi resta fuori scope da #329 e rimandata a un task successivo, quando l'auth reale sarà più diffusa tra gli utenti.

**Fix `is_ex_vsd` + Roster Admin (status/role/hard-delete) — #343/#344/#345, 19/09/2026, segnalato da Demetrio.** Due bug scoperti nel Roster live dopo il cutover #329: un ex pilota (display_name "Andrea A.") risultava scomparso dalla sezione Ex Piloti, e un tentativo di auto-impostarsi come inattivo non aveva alcun effetto visibile sul sito. Causa 1: `roster-list/index.ts` esponeva il campo come `is_ex_driver`, ma OGNI consumer frontend (Roster.jsx, DriverCard.jsx, TeamRecords.jsx, Compare.jsx, DriverProfile.jsx, Landing.jsx, LapsDrilldown.jsx, BestLaps.jsx, Calendar.jsx, driverStatus.js, RaceResultsSection.jsx) si aspetta `is_ex_vsd` — campo sempre `undefined`, quindi ogni ex pilota rimosso finiva dentro `activeDrivers` invece che `exDrivers`. **Fix**: alias nella risposta (`is_ex_vsd: !!d.removed_at`), mai il frontend — stesso principio delle fix driver_id #329/#330/#331. Deployato `roster-list` v4. Causa 2, più profonda: dopo il cutover #329 nessuna Edge Function scriveva `status`/`role`/`removed_at` su Supabase — l'unico modo per "cambiare stato" a un pilota era editare il vecchio Google Sheet, non più sincronizzato in tempo reale col sito da quel momento (`roster-update-self` blocca deliberatamente questi campi, sia a livello applicativo sia via GRANT colonna DB). **Fix**: nuove azioni `roster.adminUpdate` (staff/admin: status/removed_at/race_number; role SOLO admin), `roster.deletionCandidates`/`roster.adminDelete` (SOLO admin, richiesto separatamente da Demetrio per eliminare definitivamente ex piloti "fantasma" mai attivi — vedi sotto). Un tentativo di deploy come Edge Function standalone (`roster-admin-update`) ha colpito di nuovo il tetto 100/100 (`PaymentRequiredException`) — stesso principio di deviazione già consolidato da #259 in poi: le tre azioni sono state aggiunte come branch nel dispatcher consolidato `cloud/functions/social-manager/index.ts` (slug `endurance-auditions-get`, ora v9). Il file locale orfano `cloud/functions/roster-admin-update/index.ts` resta su disco come riferimento ma non corrisponde a nulla di deployato.

`roster.adminDelete` — audit FK completo (`information_schema`, 19/09/2026): OGNI foreign key che referenzia `drivers.id` nello schema è `CASCADE` o `SET NULL`, nessuna `RESTRICT` — il DB non offre alcuna protezione nativa contro la cancellazione di un pilota con dati reali. Costruito un controllo applicativo esplicito (`findDriverIdsWithContributions`, 20 coppie tabella/colonna: best_laps, best_lap_submissions, race_results, race_reports, race_rsvps, race_crews, clash_participants, clash_results, championship_interest, endurance_participants, endurance_stints, fuel_log, fuel_live_pings, incident_reports ×2, incident_resolutions, lap_data, pitwall_sessions, report_reactions, session_rsvps, skill_index_history — escluse deliberatamente le colonne "attore" puramente amministrative tipo verified_by/created_by e la tabella `consents`) che deve restituire zero risultati prima che `adminDelete` proceda. `adminDelete` ri-verifica SEMPRE lato server, mai fidandosi della `deletionCandidates` list letta in precedenza dal client.

Frontend wiring completato in questa sessione: `src/api/supabaseApi.js` (le tre azioni aggiunte a `SOCIAL_DIRECT_ACTIONS`), `src/api/client.js` (aggiunte a `SUPABASE_MIGRATED_ACTIONS` + metodi `api.roster.adminUpdate/deletionCandidates/adminDelete`), `src/hooks/useRoster.js` (nuovi hook `useAdminUpdateDriver`/`useDeletionCandidates`/`useAdminDeleteDriver`), nuovo componente `src/components/profile/AdminDriverPanel.jsx` integrato in `DriverProfile.jsx` — visibile a QUALSIASI viewer staff/admin su qualunque profilo (incluso il proprio, per correggere esattamente il bug segnalato): status/ex-VSD/role in un form, più una "zona eliminazione" (solo admin, solo su un pilota già ex-VSD) che invoca `adminDelete` con conferma a due click. Validato end-to-end via Chrome nella sessione successiva: pannello apre correttamente su `/roster/VSD005`, mostra status/ruolo reali dal DB, zona eliminazione correttamente condizionale a `is_ex_vsd`. Vedi task #345, chiuso.

**"Aggiungi pilota" — #346/#347/#348, 19/09/2026, richiesto da Demetrio.** Gap scoperto rispondendo alla sua domanda "dove aggiungo nuovi piloti adesso?": il trigger `link_driver_on_signup` (`005_link_discord_signup.sql`) collega apposta un login Discord SOLO a un driver già esistente (commento esplicito nel file: "Mai crea un driver nuovo") — non c'era alcun endpoint di creazione, l'unico modo era un INSERT SQL diretto (come in #238). **Backend**: due nuove azioni nello stesso dispatcher (`social-manager/index.ts`, v10) — `roster.availableSlots` (staff/admin, sola lettura: calcola il prossimo `driver_code` libero riusando i buchi lasciati da un hard-delete tipo VSD001/VSD025, e restituisce i `race_number` già assegnati + un campione di liberi 1-99, rispondendo anche alla richiesta esplicita "un sistema automatico che mi dica quali race_number e quali driver_id siano disponibili") e `roster.adminCreate` (staff/admin: `display_name` obbligatorio, `driver_code` auto-assegnato se omesso, `race_number` validato contro duplicati, `role` forzato a `driver` se il chiamante non è admin — uno staff non può crearsi un admin fittizio). **Frontend**: `supabaseApi.js`/`client.js`/`useRoster.js` estesi con le due azioni, nuovo componente `src/components/profile/AddDriverPanel.jsx` (+ CSS, tema ciano per distinguerlo dal pannello ambra di gestione) con pulsante "➕ Aggiungi pilota" in `Roster.jsx`, visibile a staff/admin, che pre-compila driver_code/mostra i numeri liberi e blocca il submit su un numero gara già in uso. **Non ancora fatto**: validazione end-to-end via Claude in Chrome di un inserimento reale (creazione + comparsa immediata nel roster) — bash ancora wedged in questa sessione, verifica rimandata al prossimo giro con l'utente o a un test manuale.

**Problema architetturale trovato e risolto**: `roster.list`/`roster.get`/`roster.updateSelf` (porting originale #239) richiedono TUTTE `Authorization` — ma la RLS su `drivers`/`drivers_public` nega esplicitamente ogni privilegio ad `anon` (`004_harden_privileges.sql`, deciso deliberatamente in #177: "anon non deve avere NESSUN privilegio su questo dominio"). Sul sito reale, però, `/roster` è pubblica (nessun `<ProtectedRoute>`) — un visitatore anonimo la vede oggi senza login (Apps Script costruisce comunque un ctx anonimo, stesso pattern già trovato in #259 per `stints.list`). Un primo tentativo di soluzione ("concedere SELECT su `drivers_public` ad anon") è stato scartato dopo aver letto `current_driver_team_id()` (`001_foundation.sql`): la funzione risolve il team leggendo `auth.uid()`, che per un chiamante anonimo è sempre NULL — la vista `drivers_public` filtra con `team_id = current_driver_team_id()`, quindi anche con il grant restituirebbe comunque zero righe. **Fix reale**: `roster-list`/`roster-get` riscritte per riusare lo stesso pattern già consolidato in #254/#255/#261 (Clash of Classes/ChampionshipInterest/Showcase) — client SERVICE ROLE che bypassa la RLS, team risolto dalla sessione se presente, altrimenti da `team_slug` esplicito nel payload (nessuna riga/colonna in più esposta rispetto a prima: stesse colonne pubbliche, stesso check self/staff per il dettaglio privato, ora applicativo invece che strutturale via RLS perché il service-role bypassa la RLS per definizione). Redeployate (`roster-list` v3, `roster-get` v3). `roster.updateSelf` NON toccata — resta l'unica azione del dominio che richiede davvero una sessione Supabase reale (scrittura self, chiama `auth.getUser()`), quindi resta instradata su `realApi.js`/Apps Script per non rompere il form "modifica profilo" per chi non ha ancora rifatto login — cutover di quell'azione rimandato a quando l'auth reale sarà più diffusa (possibilmente #341).

**Secondo bug trovato durante lo stesso giro di verifica**: tutte le Edge Function di questo progetto sono deployate con `verify_jwt=true` (default piattaforma) — senza un JWT valido in `Authorization`, la richiesta viene rifiutata dal GATEWAY Supabase prima ancora che il codice della funzione (incluso il ramo `team_slug`) venga eseguito. `supabaseApi.js` prima di questa correzione, senza sessione utente, non inviava alcun header `Authorization` — quindi OGNI chiamata anonima a QUALSIASI Edge Function (non solo Roster) avrebbe fallito a livello di gateway, non solo le azioni senza `team_slug`. Fix: `supabaseClient.js` esporta ora `supabaseAnonKey`; `callEdgeFunction` invia sempre `Authorization: Bearer <access_token o anon key>` (la anon key è comunque un JWT valido, firmato col JWT secret del progetto) e inietta `team_slug: 'vsd'` (hardcoded, progetto a singolo team) per le azioni in `ANON_TEAM_SLUG_ACTIONS` (`roster.list`, `roster.get`, `showcase.summary`, `showcase.mediaKit`) quando non c'è sessione.

**Cutover applicato in `client.js`**: nuovo switch `SUPABASE_MIGRATED_ACTIONS` in `call()` — solo `roster.list`, `roster.get`, `showcase.summary`, `showcase.mediaKit` passano da `supabaseApi.js`; tutto il resto resta su `realApi.js`, invariato. `showcase.*` non ha ancora nessuna pagina reale che la chiama sul sito live (`useShowcase.js` esiste ma nessuna rotta in `App.jsx` la usa) — incluso nel cutover senza rischio, per completare comunque lo scope del dominio pubblico.

**Bug segnalato da Demetrio, 21/09/2026: classifica Silverstone R1 (Clash of Classes) non si popolava.** Diagnosi (nessun codice toccato): `raceResults.import` aveva scritto correttamente 12 righe race + 11 qualifying in `race_results` per RACE028 — non era un problema di import. Il vero problema è che questo evento (`event_type='Clash of Classes'`, non `'championship'`) alimenta DUE pagine che leggono da fonti completamente separate e mai collegate a `race_results`: (1) `/clash-of-classes` legge da `clash_results`, scritta SOLO da `clash.results.submitRound` (`AdminClashResults.jsx`, sottomissione manuale per round) — tabella vuota, mai sottomessa; (2) `/championships/chmp-lmu-gte-vs-gt3-clash-2026` (la "schermata campionato" generica) fa fallback a un compute da `race_results` ma filtra esplicitamente `event_type='championship'` — RACE028 non passa quel filtro per design, quindi resta sempre vuota per un evento Clash, indipendentemente da quanti risultati vengano importati. **Non è un bug di porting**: `race-results-import` non ha mai scritto in `clash_results`/`standings_json`, fedele alla separazione architetturale già documentata in #254 (Clash of Classes è un dominio a parte, pensato per essere pubblico/community, non un campionato normale). **Fix applicato**: tradotti a mano i 12 risultati già presenti in `race_results` (posizione class-relative, overall calcolato per tempo totale unendo le due classi, pole/giro veloce di classe da qualifica/gara) nel contratto di `clash-results-submit-round` e sottomessi per `round=1` via `fetch` autenticato da un tab Chrome reale (bash sandbox non raggiunge Supabase, stesso limite già noto). Verificato end-to-end su `vsd-paddock.vercel.app/clash-of-classes`: classifiche GTE (7 piloti) e LMGT3 (5 piloti) popolate correttamente con punti coerenti alla griglia P1-P15+bonus pole/giro veloce/finisher del regolamento. **Richiesto subito dopo da Demetrio: far compilare anche la schermata "Campionati" generica.** Letto `standings-by-championship/index.ts` (deployato, mai committato — colmato il gap qui): la query `rounds` filtrava `championship_id` E `event_type='championship'`. Verificato via SQL su TUTTE le righe reali di `races` (`select distinct event_type, count(*) from races group by event_type`): ogni riga con `championship_id` valorizzato ha SEMPRE `event_type` uguale a `'championship'` oppure a una delle due varianti Clash (`'Clash of Class'`/`'Clash of Classes'`, campo libero senza enum, refuso di battitura tra le tre gare Clash stesse) — mai nient'altro; ogni riga con un `event_type` diverso (`4fun`, `8h Daytona`, `IMSA`, test cancellato) ha invece SEMPRE `championship_id` null. Il filtro su `event_type` era quindi ridondante rispetto a `championship_id` per ogni caso reale tranne uno: escludeva SOLO le gare Clash of Classes, senza alcun beneficio (nessuna gara "non di campionato" ha mai un championship_id da filtrare via). **Fix**: rimosso `.eq('event_type', 'championship')` dalla query `rounds`, lasciando `championship_id` come unico scoping (già sufficiente e corretto). Ridepolyato (`standings-by-championship` v3) e salvato il file canonico in `cloud/functions/standings-by-championship/index.ts` (mai presente su disco finora, gap pre-esistente colmato). Verificato via Chrome: `/championships/chmp-lmu-gte-vs-gt3-clash-2026` ora mostra le classifiche LMGT3 (5 piloti, punti 21/17/15/13/11) e LMGTE (7 piloti) coerenti con `/clash-of-classes`, più i 3 round elencati con lo stato corretto (R1 completed, R2/R3 scheduled). Nessun'altra pagina risulta impattata (nessun consumer filtra mai su `races.event_type` per uno scopo diverso da questo).

**Bug trovato in produzione durante la prima validazione live e corretto**: al primo deploy, `/roster` sul sito reale andava in pagina bianca — `TypeError: (e.preferred_sims || "").split is not a function`. Causa: `drivers.preferred_sims`/`specialties` sono `text[]` nativi su Postgres (deviazione deliberata e documentata fin da `001_foundation.sql`: "era CSV in una cella, qui array nativo"), ma `DriverCard.jsx`/`DriverProfile.jsx` — componenti mai esercitati contro il backend Supabase finché `roster.*` parlava solo con Apps Script — fanno `(driver.preferred_sims || '').split(',')`, aspettandosi la stringa CSV che il foglio Google restituiva. Un array nativo mandato lì crasha React in fase di render. **Fix in `supabaseApi.js`** (non nel frontend, coerente col ruolo dell'adapter): nuovo helper `normalizeRosterDriver()` converte `preferred_sims`/`specialties` da array a CSV prima di restituire i dati, applicato sia in `roster.list` sia in `roster.get`. Questo bug non poteva emergere dalla sola lettura statica del codice backend (la Edge Function e lo schema erano corretti rispetto al loro stesso contratto) — è emerso solo validando il vero componente pubblico via Chrome, motivo per cui la metodologia del progetto richiede sempre questo passaggio prima di chiudere un dominio.

**Validato end-to-end in produzione (commit `99ee390`), sia autenticato sia anonimo**: `/roster` (29 piloti, filtri stato/sim funzionanti, badge SIM e 🏆/🎮 roster_track corretti, zero errori console) e `/roster/VSD005` (bio, SIM, best laps, storico gare, campionati — tutto renderizzato, zero errori) verificati via sessione admin reale nel browser. Percorso anonimo verificato SEPARATAMENTE con chiamata diretta alle Edge Function usando solo la anon key + `team_slug: 'vsd'` (nessun cookie/sessione): `roster-list` → 200, 17 driver attivi; `roster-get` su `VSD005` → 200, correttamente SENZA campi privati (`email` assente, conferma che `isSelf`/`isStaff` false per un chiamante anonimo esclude la proiezione privata); `showcase.summary` (slug `endurance-auditions-get`, dispatcher `social-manager`) → 200 con le 4 chiavi attese. Nessuna pagina reale chiama ancora `showcase.*` (`useShowcase.js` non è wired in nessuna rotta), quindi la sua validazione resta solo a livello Edge Function/backend, non da una pagina renderizzata — nessun rischio dato che non è raggiungibile da nessun visitatore oggi. `roster.updateSelf` confermato NON incluso nel cutover (resta su `realApi.js`/Apps Script, vedi nota sopra).

**Task #330 — Cutover Calendario (`teamSessions.*`, `sessionRsvp.*`) — fatto, validato end-to-end via Chrome reale.** Aggiunte a `SUPABASE_MIGRATED_ACTIONS`: `teamSessions.list/create/update/remove`, `sessionRsvp.list/set`. A differenza di Roster (#329), queste Edge Function richiedono TUTTE una sessione Supabase reale, senza alcun fallback anon/`team_slug` — per design: sono dati privati del team (chi verrà all'allenamento), non una vetrina pubblica. **Gap accettato esplicitamente con l'utente**: un pilota ancora sulla sola sessione Apps Script legacy (non ancora ri-loggato via Discord-Supabase) smette silenziosamente di vedere le sessioni team/RSVP finché non rifà login — stesso tipo di gap già accettato per `roster.updateSelf` in #329, si richiuderà da solo mano a mano che il login reale si diffonde.

**Due regressioni di produzione trovate e corrette durante la validazione di questo cutover** (non da lettura statica — dallo stesso pattern driver_id-come-UUID già visto in #329, cercato proattivamente prima di dichiarare #330 chiuso):
1. **`session_rsvps.driver_id`** in Postgres è lo UUID interno (FK `drivers.id`), ma tutto il frontend (`EntityRSVP.jsx`: `rsvps.find(r => r.driver_id === currentDriverId)`) confronta con il codice pilota (`VSD005`) — il confronto falliva sempre, senza errore visibile, rompendo silenziosamente sia il riconoscimento "la tua risposta" sia il join id→nome nella lista presenze. **Fix**: `session-rsvp-list`/`session-rsvp-set` riscritte per fare join su `drivers` e restituire `driver_code` sotto la chiave `driver_id` (stesso principio del fix roster #329, qui su tutto lo schema RSVP).
2. **`team_sessions`** ha PK `id`, non `session_id` — ma `Calendar.jsx` (`sessionToEvent`) e `AdminTeamSessions.jsx` leggono universalmente `s.session_id`, sempre `undefined` prima del fix. Effetto pratico più grave dei due: il pannello RSVP pubblico su `/calendario` riceveva `sessionId=undefined`, quindi OGNI tentativo di conferma presenza falliva con "session_id obbligatorio" — bottone apparentemente funzionante ma silenziosamente rotto per qualsiasi pilota. **Fix**: `team-sessions-list/create/update` riscritte per alias `id`→`session_id` e join `drivers` per `created_by`→`driver_code` (usato da `AdminTeamSessions.jsx` per decidere se mostrare il tasto elimina all'autore della sessione).

Entrambi i fix seguono lo stesso principio già stabilito: alias nella risposta dell'Edge Function, mai toccare lo schema o i 40+ file frontend consumer. **Validato end-to-end in produzione** (sessione admin reale, sessione team Barcellona 31 ago): pannello RSVP con `sessionId` valido, round-trip completo di cambio stato (CI SARÒ → FORSE → CI SARÒ, ogni transizione confermata dal conteggio "N/29 confermati" e dai nomi in lista, poi ripristinato allo stato originale), tasto elimina visibile in `/team-sessions` per la sessione (creata da admin, quindi comunque visibile, ma senza il crash che un `created_by` non allineato avrebbe causato).

**Task #331/#342 — Cutover Best Laps/Academy/Records/Training/Submissions/Garage61 — fatto, validato end-to-end via Chrome reale.** Aggiunte a `SUPABASE_MIGRATED_ACTIONS`: `laps.list/leaderboard/raceLaps/syncFromGarage61/add/update/remove`, `lapSubmissions.submit/listMine/listPending/approve/reject/remove`, `academy.ranking`, `records.team`, `training.insights`. Stesso gap accettato di #330 (nessun fallback anon/`team_slug`, tutte queste Edge Function richiedono sessione Supabase reale — un pilota ancora sulla sola sessione Apps Script legacy smette silenziosamente di vedere dati finché non rifà login).

**Bug di produzione trovato e corretto durante la validazione di questo cutover — ambiguous embed PostgREST**: il fix driver_id→driver_code di #331 (scritto in una sessione precedente non tracciata da questo tracker) usava la sintassi di embed implicito di PostgREST, `.select('*, drivers(driver_code)')`. Questa sintassi è ambigua — e PostgREST la RIFIUTA con HTTP 400 (`"Could not embed because more than one relationship was found for 'X' and 'drivers'"`) — ogni volta che la tabella sorgente ha PIÙ DI UNA foreign key verso la stessa tabella target. Verificato via query diretta su `pg_constraint`: `best_laps` ha due FK verso `drivers` (`driver_id` e `verified_by`), `best_lap_submissions` ne ha altrettante (`driver_id` e `reviewed_by`). Il bug era silenzioso finché `client.js` non ha iniziato a instradare queste azioni su Supabase (questo stesso cutover) — a quel punto `/laps`, la leaderboard e la lista submission pendenti avrebbero iniziato a fallire con 400 per OGNI chiamata. Diagnosticato NON dalla sola UI (che mostrava semplicemente "Nessun giro trovato", senza errore visibile) ma da una chiamata `fetch()` diretta all'Edge Function dal contesto della pagina, bypassando il layer react-query — questo isolamento ha permesso di individuare l'errore 400 reale invece di fermarsi al sintomo. **Fix**: sostituita la sintassi di embed implicito con l'hint esplicito di relazione, `drivers!<nome_esatto_del_vincolo_fk>(driver_code)`, in `best-laps-list` (v3), `best-laps-leaderboard` (v3), `best-laps-update` (v3) e nell'azione `lapSubmissions.listPending` del dispatcher `endurance-auditions-get`/`social-manager/index.ts` (v11). **Gotcha da tenere a mente per i domini ancora da portare (#332+)**: qualunque tabella con più di una FK verso `drivers` (es. `race_reports.driver_id` + eventuali colonne "attore" come `reviewed_by`/`created_by`/`verified_by` sulla stessa tabella) romperà silenziosamente un embed implicito `drivers(driver_code)` — usare sempre l'hint esplicito `drivers!<fk_name>` quando si applica questo pattern di alias. `team_sessions`/`session_rsvps` (#330) NON sono a rischio: verificato via `pg_constraint` che hanno una sola FK ciascuna verso `drivers`.

**Validato end-to-end in produzione** (sessione admin reale): chiamata diretta alle tre Edge Function corrette + al dispatcher (prima del fix: 400 su tutte e tre; dopo: 200 ok su tutte, incluso `lapSubmissions.listPending`), `laps.list` conferma 461 giri con `driver_id` correttamente aliasato a codice pilota (es. `VSD018`, non l'UUID interno). Verificate visivamente via Chrome: `/laps` (tabella Leaderboard popolata, nomi piloti reali, stato "verificato"), `/records` (Muro dei Record per pista/categoria, nomi e tempi reali), `/training` (elenco piloti con ultimi 7g/30g e data ultima sessione, calcolato da Best Laps). `/academy` (Punti Merito) mostra correttamente "Nessun risultato" per ogni sim — verificato via query diretta che `race_results` ha 0 righe in Supabase: `academy.ranking`/questo output dipendono dal dominio Races/RaceResults (#332), non ancora migrato/importato, quindi il vuoto è atteso e non un difetto di questo cutover.

**Task #349 — Migrazione dati storici Races/RaceResults/Championships verso Supabase, 20/09/2026.** Prima di poter anche solo valutare il cutover #332, un controllo (`execute_sql` count su `races`/`race_results`/`incident_reports`/`championships`/`skill_index_history`) ha rivelato che TUTTE queste tabelle erano completamente vuote su Supabase — nessuno storico era mai stato migrato dai Google Sheet legacy, a differenza di Roster/Calendario/Best Laps (#245/#247/#248). Cutover a tabelle vuote avrebbe reso pagine pubbliche live (Race Hub, Risultati, Campionati) improvvisamente vuote per i visitatori reali — **fork architetturale riconosciuto e segnalato a Demetrio** (non proceduto in autonomia), che ha scelto "Migra prima i dati storici". Eseguita una migrazione one-off dal backend Apps Script live (`VITE_API_URL`) direttamente a Supabase via `fetch()` dal browser (sessione admin con token Apps Script E sessione Supabase Auth simultanei in `localStorage`) verso l'API REST PostgREST (`/rest/v1/<table>`, header `Prefer: resolution=ignore-duplicates`), sfruttando le policy INSERT staff/admin già esistenti — evitato così sia un giro service-role sia il blocco del filtro contenuti del tool browser su alcuni valori grezzi (`poster_url`) che impediva di far transitare l'intero payload attraverso il canale di risposta del tool. Risultato: **9 championships**, **49 races** (55 righe sorgente, 6 duplicati `race_id` pre-esistenti nel Google Sheet stesso — non un artefatto della migrazione, verificato con un conteggio dedicato), **1479 race_results**. Mapping applicato per i valori non ammessi dai check constraint Postgres: `races.status` (`draft`→`scheduled`), `championships.status` (`scheduled`→`upcoming`), `race_results.session_type` (`practice`/`warmup`→`race`). Ordine di insert vincolato dalla FK `races.championship_id`→`championships.id` (championships prima). `incident_reports`/`skill_index_history` restano vuote deliberatamente: la prima non ha mai avuto dati reali nemmeno lato legacy (vedi nota Incidents sotto), la seconda è uno snapshot on-demand senza equivalente storico da migrare (il sorgente Apps Script calcola sempre live da `race_results`, non salva snapshot allo stesso modo).

**Task #332 — Cutover Races/RaceResults/Championships/Standings/SkillIndex/SeasonRecap — fatto (Incidents ESCLUSO, vedi sotto), validato end-to-end via Chrome reale.** Aggiunte a `SUPABASE_MIGRATED_ACTIONS`: `races.list/upcoming/get/updatePoster/updateGallery/add/update/remove`, `raceResults.list/import`, `championships.list/importStandings/saveAdjustments`, `standings.byChampionship/byDriver/progression`, `skillIndex.list/history`, `recap.mine`. Stesso gap accettato di #330/#331 (nessun fallback anon/`team_slug`).

**Fix driver_id→driver_code, stesso pattern #329-331, applicato a 6 Edge Function prima del cutover**: `race-results-list` (esponeva l'uuid raw, nessun aliasing — stesso bug-pattern di `best-laps-list` pre-#331; ora accetta anche `driver_id` in ingresso come driver_code, risolto a uuid per il filtro), `standings-by-championship` (il calcolo interno di matching/merge/aggiustamenti resta sull'uuid interno per compatibilità con `race_results.driver_id`, ma l'output viene aliasato a driver_code come ULTIMISSIMO step prima di ogni `return` — necessario perché `ChampionshipDetail.jsx` fa `driverMap[s.driver_id]` con una mappa keyed by driver_code da `useRoster()`), `standings-by-driver` (accettava `driver_id` come se fosse già un uuid e faceva `.eq('id', driverId)` — ma `DriverProfile.jsx`/`Compare.jsx` passano sempre il driver_code da route param: ogni chiamata reale falliva con 404 "Driver non trovato"; ora risolve driver_code→uuid subito dopo l'auth, usa l'uuid per tutte le query interne, ritorna driver_code in output), `standings-progression` (stesso alias in uscita su `series[].driver_id`, nessun consumer diretto lo usava ancora ma allineato per coerenza), `skill-index-list` (esponeva l'uuid raw — `useSkillIndex.js` fa `drivers.find(d => d.driver_id === driverId)` con un driverId che è driver_code: falliva sempre silenziosamente), `skill-index-history` (stesso bug di `standings-by-driver`: `.eq('driver_id', driverId)` con `driverId` driver_code contro una colonna DB uuid — zero righe sempre). `season-recap` verificato e confermato SENZA bisogno di fix: non accetta/espone mai un `driver_id` esterno, opera solo su `me.id` (l'utente autenticato stesso).

**Gap chiuso in `races-list`/`races-upcoming`/`races-get`**: `championship_name` era sempre `null` (v1, scritto in Fase 1 quando il dominio Championships non esisteva ancora) — usato realmente da `NextRaceHero.jsx` (badge sulla card "prossima gara"), `Calendar.jsx` (raggruppamento/etichetta), `AdminImportResults.jsx` (disambiguazione nel picker gare). Ora popolato via lookup su `championships` per `championship_id`, ora che il dominio esiste ed è popolato.

**Dominio Incidents (`incidents.list/resolve/report`) DELIBERATAMENTE ESCLUSO da questo cutover — task #351 aperto per la decisione.** Durante l'audit, la lettura completa di `apps-script/Incidents.js` (sorgente legacy) ha rivelato un'architettura fondamentalmente diversa da quella già deployata su Supabase, non un semplice bug driver_id: il sistema reale legge le segnalazioni LIVE da un **Google Form esterno separato** (`RECLAMI_SPREADSHEET_ID`, foglio "VSD - Modulo reclamo (Risposte)", parsato per posizione colonna), unito a runtime con un tab `IncidentResolutions` tramite `complaint_key` (il timestamp della riga del Form) — non esiste alcun concetto di tabella Postgres "incident_reports" nel sistema legacy, le segnalazioni non vengono mai scritte dall'app. Le Edge Function `incidents-*` attualmente deployate usano invece `report_id`/tabelle Postgres proprie con nomi campo diversi (`track_id` vs `track`, `championship_id` vs `championship`, mancano interamente `timestamp`/`reporter_sim`/`reporter_discord`/`verdict`/`verdict_en` che `AdminIncidents.jsx` usa direttamente per il render e per costruire il payload di risoluzione) — non un porting fedele, un dominio diverso da riprogettare. `incidents.report` inoltre non è nemmeno wired in `client.js` oggi. Sia `incident_reports` sia `incident_resolutions` sono vuote su entrambi i lati (nessun incidente mai formalizzato), il che abbassa l'urgenza ma non cambia la natura del gap. Opzioni per Demetrio (task #351): (a) lasciare Incidents su Apps Script a tempo indeterminato, (b) riscrivere le tre Edge Function per il vero pattern ibrido Google-Form + tab risoluzioni, (c) altra soluzione — nessuna scelta ancora presa.

**Validato end-to-end in produzione** (sessione admin reale via Chrome): `/race` (Race Hub, 12 programmate + 42 storico, dati e date reali), `/results` (Risultati Gare, nomi pilota reali non uuid, filtri sim/classe/tracciato popolati, es. Simone R./Ivan F./Paolo T. su Sebring LMP2/LMGT3), `/championships` (9 campionati: 4 in arrivo + 5 conclusi, coerente col conteggio migrato in #349), `/championships/chmp-lmu-ultimate-endurance-144-2026` (classifica per classe con badge VSD correttamente separati dai piloti esterni via l'alias driver_code, grafico "Andamento Punti" popolato), `/roster/VSD011` (Ivan F.: 15 gare disputate, 3 podi, posizione media 8.2, 415 punti stagione, Indice Skill 37 su 15 gare cross-sim, grafico "Andamento Indice Skill" popolato, tabella Campionati con 3 partecipazioni) — confermato via chiamata diretta alle Edge Function che tutti i dati erano corretti anche PRIMA che il browser mostrasse i numeri giusti: la prima visita a `/roster/VSD011` mostrava erroneamente "0 gare" per cache react-query stantia da una navigazione precedente al deploy dei fix, risolto con un hard-reload — non un bug residuo.

**Task #333 — Cutover Clash of Classes — fatto, validato end-to-end via Chrome reale.** Aggiunte a `SUPABASE_MIGRATED_ACTIONS`: `clash.participants.list/register/add/update/remove`, `clash.standings`, `clash.results.submitRound`, `clash.incidents.report/list`. A differenza di #330-332, quattro di queste azioni sono pubbliche per design fin dal sorgente legacy (community non tesserata, cap. 2.2 regolamento): `clash.participants.list`, `clash.participants.register`, `clash.standings`, `clash.incidents.report` — aggiunte a `ANON_TEAM_SLUG_ACTIONS` in `supabaseApi.js` (mancavano, gap trovato per lettura statica prima del deploy: senza, ogni visitatore anonimo sulla pagina pubblica avrebbe ricevuto "team_slug obbligatorio per chiamate anonime"). Le altre 5 (add/update/remove partecipanti, submitRound, incidents.list) restano staff/admin, stesso gap accettato di #330-332 (sessione Apps Script legacy non ancora ri-loggata perde silenziosamente l'accesso finché non rifà login).

**Bug più profondo del solito trovato durante l'audit pre-cutover — non un mismatch silenzioso ma un errore Postgres di tipo**: `clash_participants.driver_id`/`clash_results.driver_id` sono colonne `uuid` native (upgrade deliberato da testo libero a FK vera durante lo schema design, #278) — ma il contratto pubblico (confermato in `apps-script/ClashOfClasses.js`, righe 170/269/505) e il form staff (`AdminClashResults.jsx`, placeholder "driver_id VSD (opz.)") si aspettano sempre un driver_code (`VSD005`), mai l'uuid interno. Senza risoluzione, digitare un driver_code nel form staff avrebbe fatto fallire l'insert con un errore Postgres di tipo grezzo ("invalid input syntax for type uuid"), non il solito "0 risultati" silenzioso degli altri domini. **Fix**: `clash-participants-add`/`clash-results-submit-round` (v2) risolvono driver_code→uuid scoped al team PRIMA di ogni insert, con errore chiaro (`400 "driver_id non trovato nel roster: <code>"`) se il codice non esiste — verificato in produzione con una chiamata diretta con un codice inesistente, risposta pulita senza scrivere nulla. Aliasing uuid→driver_code applicato anche in uscita su `clash-participants-list/register/update` e `clash-standings` (v2), stesso principio #329-332.

**Regressione di produzione trovata e corretta subito dopo il primo deploy del cutover — dati storici mai migrati**: a differenza di Races/Championships (#349), nessuno aveva mai migrato le iscrizioni reali di Clash of Classes dal Google Sheet legacy a Supabase prima di questo cutover. Il primo deploy ha quindi mostrato **0/22 iscritti** sulla pagina pubblica `/clash-of-classes`, contro l'11/22 (6 GTE, 5 LMGT3) reale visibile un minuto prima via Apps Script — regressione live, notata subito dopo il push e **segnalata a Demetrio** (non corretta in autonomia, data la vicinanza a Round 1 di oggi, 20/09/2026), che ha scelto di migrare subito i dati invece di fare rollback temporaneo. Recuperate le 11 iscrizioni reali via chiamata diretta autenticata a `clash.participants.list` sul backend Apps Script live (stesso pattern generale di #349, qui via il vecchio endpoint invece che PostgREST), risolti i 6 driver_id con corrispondenza nel roster VSD (VSD005/VSD026/VSD017/VSD011/VSD020/VSD008 → uuid) e inserite tutte e 11 le righe in `clash_participants` via `execute_sql` (5 piloti community esterni al roster con `driver_id NULL`, coerente con la FK `ON DELETE SET NULL`). Verificato post-fix: conteggio Supabase torna 6 GTE + 5 GT3, pagina pubblica torna a mostrare 11/22 corretto.

**Validato end-to-end in produzione** (sessione admin reale via Chrome, dopo il fix dati): `/clash-of-classes` (pagina pubblica — 11/22 iscritti, 6 GTE/5 LMGT3, form iscrizione e classifiche renderizzati, zero errori console applicativi), chiamata diretta a `clash-participants-list` conferma `driver_id` correttamente aliasato a driver_code per tutti e 6 i piloti roster (`VSD005/VSD026/VSD017/VSD011/VSD020/VSD008`) e stringa vuota per i 5 community esterni; `/admin/clash-results` (tab Risultati — 11 piloti con classe corretta; tab Iscritti — colonna DRIVER_ID mostra i codici corretti, non uuid); chiamata diretta a `clash-participants-add` con un driver_id inventato conferma `400` pulito senza scrittura. Classifiche non ancora testabili end-to-end via UI (Round 1 non ancora disputato al momento della validazione — "Stagione in arrivo" è lo stato atteso), ma `clash-standings` risponde 200 e applica correttamente l'aliasing sulla struttura vuota.

**Task #351 — Incidents: gap architetturale risolto con form nativo in-app, sostituendo il Google Form esterno — fatto, validato end-to-end via Chrome reale.** Il gap documentato sopra (righe 130) è stato chiuso non replicando il vecchio Google Form (opzione fedele-al-sorgente) ma **ritirandolo**: decisione esplicita di Demetrio ("Migra a un form nativo in-app", 20/09/2026), perché UE144 è una lega multi-team aperta a tutti, non solo VSD, e un form nativo dentro vsd-paddock è più coerente col resto del sito rispetto a un link esterno Google. Schema `incident_reports` esteso via migrazione `incidents_native_form_redesign` (`reporter_driver_id` reso nullable — un segnalante anonimo non ha un driver interno; nuove colonne `reporter_sim`/`reporter_discord`/`against`; rimossa `against_name_external`, ridondante con `against`), applicata a tabelle confermate vuote (0 righe) prima della modifica.

Tre Edge Function riscritte per il nuovo contratto reale (non il vecchio design "REST generico" che non rifletteva né il sorgente né il frontend):
- **`incidents-report` (v3)** — pubblica, stesso pattern anon+team_slug di Clash of Classes/ChampionshipInterest: auth opzionale (se presente, `reporter_driver_id` e `reporter_sim` di default risolti dalla sessione), altrimenti richiede `team_slug`. Match best-effort di `against` contro il roster (ilike su `display_name`, mai bloccante). **Bug trovato in validazione live (v2→v3)**: `championship_id` è una FK composita reale su `championships(team_id, id)` (aggiunta in #252), non testo libero — il primo invio di test ha fallito con `insert or update on table "incident_reports" violates foreign key constraint "incident_reports_championship_fkey"` perché il payload passava `"UE144"` (etichetta breve) invece dell'id reale (`chmp-lmu-ultimate-endurance-144-2026`). Fix a due livelli: `UE144.jsx` ora passa la costante `UE144_CHAMPIONSHIP_ID` già esistente nel file (mai usata prima per questo) invece della stringa letterale; l'Edge Function in v3 risolve comunque `championship` con una lookup scoped a `team_id` e non blocca mai l'invio se non trova corrispondenza (`championship_id` resta `NULL`) — stessa filosofia non-bloccante di `against_driver_id`, per non far mai perdere una segnalazione reale per un valore di metadata sbagliato.
- **`incidents-list` (v3)** — richiede sessione autenticata. **Fresh instance dello stesso bug driver_id/uuid già visto in #329-333, catturata qui in code review PRIMA del deploy**: la prima bozza esponeva `reporter_driver_id`/`against_driver_id`/`resolved_by`/`penalized_driver_id` come uuid grezzi — incrociando `DriverProfile.jsx` (`authDriver.driver_id === driverId`, `driverId` da route param = driver_code) e `MyIncidentsPanel.jsx` (`useMyIncidents` filtra su `i.reporter_driver_id === driverId`) sarebbe stato lo stesso bug silenzioso di `best-laps-list`/`standings-by-driver` ecc., mai spedito in produzione. Fix: mappa `codeByUuid` costruita sui driver del team, applicata a tutti e 4 i campi in uscita.
- **`incidents-resolve` (v2)** — sostituisce una v1 che si aspettava `payload.report_id` mentre `AdminIncidents.jsx` ha sempre inviato `payload.complaint_key` (mai esercitato prima, essendo `incidents.resolve` mai stato in `SUPABASE_MIGRATED_ACTIONS`). v2 accetta `complaint_key`, valida `status`, e risolve `penalized_driver_id` da driver_code a uuid PRIMA dello upsert (stesso pattern di `clash-participants-add`), con `400 "penalized_driver_id non trovato nel roster: <code>"` se il codice non esiste.

Frontend: `client.js` (`incidents.report` aggiunto, `SUPABASE_MIGRATED_ACTIONS` aggiornato), `supabaseApi.js` (`incidents.report` in `ANON_TEAM_SLUG_ACTIONS`), nuovo hook `useReportIncident` in `useIncidents.js`, nuovo componente condiviso `IncidentReportSection.jsx` (riusa `AciLmgt3Challenge.module.css`, stesso principio di `ChampionshipInterestSection`) montato in `UE144.jsx` al posto del vecchio link esterno (`PROTEST_FORM_URL` rimosso, commento storico lasciato).

**Bug UI pre-esistente trovato e corretto durante la validazione, non causato da questo dominio**: `AdminIncidents.jsx`/`IncidentCard` inizializza il proprio stato locale del form (`status`, `penaltyType`, `sim`, `penalizedDriverId`, ecc.) con `useState(inc.xxx)` — inizializzazione che React esegue solo al mount. Poiché la card è chiave `key={inc.complaint_key}` (mai cambia), dopo un salvataggio riuscito React NON rimonta il componente: il form torna a mostrare valori vuoti anche se il backend ha salvato correttamente. Bug mai emerso prima perché `incidents.resolve` non era mai stato collegato a dati reali fino a questa sessione. Fix: chiave estesa a `inc.complaint_key + ':' + inc.resolved_at`, forzando il remount (quindi la re-inizializzazione dello stato ai valori freschi) solo quando una risoluzione viene effettivamente salvata.

**Validato end-to-end in produzione** (sessione staff reale via Chrome + chiamate dirette per il percorso anonimo, impossibile da riprodurre nella stessa sessione browser autenticata): invio segnalazione come sessione staff loggata (`reporter_sim` precompilato "Demetrio C. (roster VSD)", nessun campo nome mostrato, `reporter_driver_id` risolto correttamente a uuid interno e poi ri-aliasato a `VSD005` in lettura); invio anonimo via chiamata diretta con solo `apikey`/anon key (nessuna sessione) + `team_slug: 'vsd'` → `200`, `reporter_driver_id` correttamente `NULL` in tabella; comparsa in `/admin/incidents` con `reporter_driver_id`/`against_driver_id`/`resolved_by`/`penalized_driver_id` tutti come driver_code (mai uuid) nella risposta API; risoluzione completa (status→`reviewing`, penalty_type→`warning`, sim→`LMU`, penalized_driver_id→`VSD005`) salvata e riverificata via `execute_sql` con `penalized_driver_id`/`resolved_by` correttamente uuid in storage; comparsa in "I miei incidenti" su `/roster/VSD005` con badge "IN REVISIONE" e penalità "warning" — conferma diretta che il filtro `reporter_driver_id === driverId` di `useMyIncidents` funziona con l'aliasing corretto. Tutti i dati di test rimossi via SQL diretto a fine validazione.

**Task #334 — Cutover Interest/Prequal/Candidates/Sponsors/Treasury/Consent — fatto, validato end-to-end via Chrome reale (compresa una regressione di produzione trovata e corretta subito dopo il primo deploy).** Aggiunte a `SUPABASE_MIGRATED_ACTIONS`: le 22 azioni dei 6 sotto-domini del dominio #255 (`interest.*`, `prequal.*`, `candidates.*`, `sponsors.*`, `treasury.*`, `consent.*`). Quattro sono pubbliche per design (`interest.list/register`, `prequal.list`, `consent.socialFlags`, già note dal sorgente) e aggiunte a `ANON_TEAM_SLUG_ACTIONS`, stesso pattern di #330-333.

**Regressione di produzione reale, non un gap noto accettato — trovata dall'utente in produzione, non in validazione.** A differenza di #330-333 (dove il gap "sessione Apps Script legacy non ri-loggata" era stato documentato e accettato come noto), qui il gap si è rivelato **totale e non una tantum**: lettura di `Login.jsx`/`AuthContext.jsx` conferma che l'UNICO metodo di login reale per qualunque pilota è Discord OAuth legacy via Apps Script (`api.auth.discordStart()` → `auth.discordCallback` → token in `localStorage.vsd_paddock_token`) — nessun flusso reale crea mai una sessione Supabase (`supabase.auth`). Verificato via SQL: su 29 driver, **solo VSD005 (Demetrio, sessione creata manualmente in #327 a scopo di test/admin) ha `auth_user_id` valorizzato**. Le 19 azioni non pubbliche di questo dominio (tutte tranne le 4 sopra) richiedevano una sessione Supabase reale senza alcun fallback: il deploy ha quindi bloccato silenziosamente (401) ogni pilota reale tranne Demetrio su consenso/tesoreria/candidature/sponsor/prequalifiche/iscrizioni-modifica. **Segnalato dall'utente**: *"Adone ha compilato il consenso ma non viene registrato"* — bug reale in produzione, non un caso limite.

**Fix approvato dall'utente ("Fallback token legacy", scelto tra 3 opzioni presentate: fallback / rollback parziale / rollback totale)**: aggiunto un fallback di verifica token legacy — stesso meccanismo già usato indipendentemente in `api/media-upload.js` — a tutte le 19 Edge Function interessate (`interest-update/remove`, `prequal-add/remove`, `candidates-list/add/update/remove`, `sponsors-list/add/update/remove`, `treasury-list/add/update/remove`, `consent-status/accept/adminList`). Pattern uniforme: la funzione prova prima la sessione Supabase reale (`Authorization` → `auth.getUser()` → lookup `drivers.auth_user_id`); se assente o non risolta, verifica `payload.legacy_token` contro `auth.verify` sul backend Apps Script legacy (URL hardcoded nella funzione — non è un vero secret, è già nel bundle client pubblico via `VITE_API_URL`, e non c'è modo di impostare nuovi Edge Function secret da questa sessione) e risolve il `driver_code` risultante su un driver Supabase reale tramite un client SERVICE ROLE (bypassa RLS, stesso principio già usato per gli endpoint pubblici di Clash of Classes). Tutte le query di queste 19 funzioni sono state spostate dal client RLS-scoped (anon+Authorization) al client service-role, con lo scoping per team preservato manualmente (`.eq('team_id', me.team_id)`) e i gate di ruolo esistenti (staff/admin per candidati/sponsor/prequal/interest.remove, SOLO admin per treasury/consent.adminList, nessun gate per consent.status/accept/interest.update essendo self-service) applicati sul `role` risolto da entrambi i percorsi. Lato client, `supabaseApi.js` inietta `legacy_token` (da `localStorage.vsd_paddock_token`) nel body di queste 19 azioni quando non esiste una sessione Supabase reale (`LEGACY_TOKEN_FALLBACK_ACTIONS`).

**Secondo bug reale, indipendente dal fallback, trovato durante la validazione end-to-end**: le 4 Edge Function `treasury-*` referenziavano una tabella `treasury` inesistente — la tabella reale (creata in #255, `022_admin_domains.sql`) si chiama `treasury_entries`. Falliva per QUALUNQUE chiamante, incluso un admin con sessione Supabase reale, non solo per il fallback legacy: `treasury-list` rispondeva `Could not find the table 'public.treasury' in the schema cache`. `treasury-add` aveva inoltre un secondo mismatch: scriveva `created_by`, colonna che non esiste sulla tabella reale (solo `updated_by`). Corretto in tutte e 4 le funzioni (v3/v4), riverificato: `treasury-list` torna le 18 righe storiche reali migrate in #354 con saldo calcolato correttamente (€174,15).

**Migrazione dati storici pre-cutover (#353/#354)**: dati reali estratti direttamente dal backend Apps Script live (fetch autenticato dal browser con il token legacy reale, replicando `postToBackend()`, non una ricostruzione a tavolino) e inseriti in Supabase via `execute_sql`: 18 movimenti di cassa (treasury_entries), 12 consensi (consents, tutti non minorenni, `birth_date`/`is_minor` verificati), 9 segnalazioni di interesse campionato (championship_interest), 4 candidati prequalifica (prequal_candidates) — nessun dato storico per candidates/sponsors (mai popolati nel sorgente legacy, 0 righe confermato coerente sia prima sia dopo il cutover).

**Validato end-to-end in produzione**: sia per chiamata diretta (fetch con `legacy_token` nel body, anon key in Authorization — replica esatta di cosa manda un pilota reale senza sessione) su `consent-status`/`consent-accept`/`treasury-list`/`candidates-list`/`sponsors-list`/`consent-admin-list`/`prequal-add`, sia — più significativo — **attraverso l'app reale in produzione**: rimossa temporaneamente la sessione Supabase di Demetrio da `localStorage` (simulando esattamente lo stato di ogni pilota reale) e inviato il form "Il mio consenso" su `/consenso` dalla UI vera, non da uno script — risultato "✓ Scelte salvate", confermando che il fallback funziona end-to-end dal click dell'utente fino alla riga in Postgres. Sessione ripristinata subito dopo.

**Terzo bug reale, trovato in validazione e corretto su richiesta esplicita dell'utente ("sistemalo immediatamente")**: dopo il fix del fallback, `/consenso` mostrava comunque un form vuoto (checkbox deselezionate, data di nascita assente) anche per Demetrio, che aveva un consenso già registrato pochi minuti prima. Diagnosi iniziale sbagliata (corretta poi): non è un problema di chiamata mancante — `useConsentStatus` (`src/hooks/useConsent.js`) la richiama regolarmente. Il vero problema è un **mismatch di contratto tra `consent-status` e `ConsentForm.jsx`**: `ConsentForm.jsx` (mai riscritto durante il porting #255/#334, costruito contro il vecchio backend Apps Script) legge `statusQuery.data.has_current` e `statusQuery.data.record.consent_id`/`.birth_date`/ecc. per il pre-fill "adjust state during render" (righe 44-61), mentre la Edge Function `consent-status` v2 restituiva `has_consent`/`consent`/`consent.id` — nomi diversi, nessun errore, nessun log: il ramo di pre-fill semplicemente non scattava mai (`existing` sempre `undefined`). Fix (v3, `consent-status`): risposta riallineata al contratto storico (`has_current`, `record.consent_id` invece di `record.id`), invece di riscrivere il frontend — più sicuro, non tocca consumer non ancora auditati. Riverificato end-to-end su `/consenso` con la sessione reale di Demetrio: checkbox, data di nascita (19/06/1969) e banner "✓ Hai già registrato le tue scelte per questa versione del documento" tutti precompilati correttamente dopo il fix.

**Quarto bug reale, stesso pattern del fallback #334 ma su un dispatcher diverso — segnalato dall'utente il 20/09/2026: *"Casesi non riesce comunque ad inviare il proprio Best Laps con errore Auth richiesto"*.** Il fallback token legacy di #334 copriva solo le 19 Edge Function del dominio #255 (Interest/Prequal/Candidates/Sponsors/Treasury/Consent). Il dispatcher condiviso `social-manager` (deployato sotto lo slug storico `endurance-auditions-get`, sorgente canonico `cloud/functions/social-manager/index.ts`) risolveva `me` **solo** da `auth.getUser()` su una sessione Supabase reale — stessa causa radice di #334, ma qui il raggio d'azione è molto più ampio: questo dispatcher serve Social Manager, Race Reports/Reazioni, `landing.data`, `laps.raceLaps`, Best Lap Submissions/Garage61 e Pit Wall broadcastLive (#260-263), quindi il bug bloccava silenziosamente (401 "Auth richiesto") ogni pilota reale tranne Demetrio su TUTTE queste azioni, non solo sull'invio Best Laps dove è stato notato per primo.

**Fix**: stesso pattern di `resolveLegacyDriver` (verifica `payload.legacy_token` contro `auth.verify` sul backend Apps Script legacy, poi lookup del driver su Supabase via client service-role) aggiunto in testa a `cloud/functions/social-manager/index.ts`, applicato UNA VOLTA nel blocco di risoluzione auth condiviso in cima a `Deno.serve` (non nei singoli handler): se la sessione Supabase reale non risolve `me`, si tenta il fallback legacy e, se risolto, sia `me` sia il client `supabase` usato dal resto della richiesta passano al client service-role (RLS bypassata, scoping per team/driver preservato esplicitamente in ogni handler esattamente come già accadeva con la sessione reale). Deployato come v12 dell'Edge Function `endurance-auditions-get` (`verify_jwt: false`, invariato — necessario per le azioni pubbliche Showcase). Lato client, `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js` esteso con le azioni self-service di questo dispatcher: `lapSubmissions.submit`, `lapSubmissions.listMine`, `reports.list`, `reports.recent`, `reportReactions.list`, `reportReactions.toggle`, `landing.data`, `laps.raceLaps` — le azioni admin-only dello stesso dispatcher (Social Manager, `lapSubmissions.listPending/approve/reject/remove`, `reports.update/seedForRace`) restano fuori per ora, nessun caso reale segnalato per uno staff/admin senza sessione Supabase.

**Validato end-to-end**: chiamata diretta all'Edge Function con `legacy_token` reale (sessione Supabase di Demetrio rimossa da `localStorage`, replicando lo stato di ogni pilota vero) — `lapSubmissions.listMine` → `200 {ok:true}` (prima del fix rispondeva 401), `lapSubmissions.submit` con dati di test → `200`, submission creata con `driver_id` correttamente aliasato a `VSD005` (non l'uuid interno), record di test rimosso subito dopo con `lapSubmissions.remove`. Confermato in produzione dall'utente dopo il push del fix client ("sembra funzionare"): Casesi è riuscito a inviare il proprio tempo, visibile in coda "Da validare" nel pannello staff.

**Quinto bug reale, stesso pattern ma sull'intero dominio #331 — scoperto durante la validazione post-fix del bug precedente**: mentre si verificava in produzione che il fix Casesi avesse effetto, `best-laps-list` continuava a rispondere 401 anche per un pilota senza sessione Supabase. Causa: il fallback token legacy non era MAI stato applicato all'intero dominio #331 (Best Laps/Academy/Records/Training) — né lato Edge Function né lato client — nonostante fosse chiuso "fatto, validato end-to-end" nel tracker: la validazione originale di #331/#342 era stata eseguita con la sessione Supabase reale di Demetrio, quindi il gap (nessun fallback per chi ha solo il token legacy — cioè ogni pilota reale tranne l'admin di test) non era mai stato esercitato. Bug bloccava silenziosamente (401 "Auth richiesto") `laps.list`, `laps.leaderboard`, `laps.update`, `laps.remove`, `records.team`, `academy.ranking`, `training.insights` per chiunque non avesse rifatto login Discord via Supabase — cioè la quasi totalità dei piloti reali, su Best Laps, Muro dei Record, Academy e Training.

**Fix**: stesso `resolveLegacyDriver` applicato in testa a ciascuna delle 7 Edge Function (`best-laps-list` v4, `best-laps-leaderboard` v4, `best-laps-update` v4, `best-laps-remove` v2, `records-team` v3, `academy-ranking` v3, `training-insights` v3) — se la sessione Supabase reale non risolve `me`, fallback su `payload.legacy_token` con client service-role, stesso `me`+`supabase` propagati al resto dell'handler. Aggiunto anche scoping esplicito `.eq('team_id', me.team_id)` alle query di `best-laps-list`/`leaderboard`/`update`/`remove`/`records-team` che prima si affidavano interamente a RLS (sicuro con client anon+JWT, non sicuro col fallback service-role che bypassa RLS) — `academy-ranking`/`training-insights` già filtravano esplicitamente per `team_id`, nessuna modifica necessaria lì oltre alla risoluzione auth. Lato client, `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js` esteso con le 7 azioni.

**Validato end-to-end in produzione con la sessione Supabase reale**: dopo aver ricostruito la sessione di Demetrio (vedi nota sotto), `/laps` mostra correttamente la Leaderboard con `driver_id` aliasato a `driver_code`; `/admin/best-laps` tab "Da validare" mostra correttamente il tempo di Casesi in coda, niente più "Accesso riservato ad admin/team principal". La validazione diretta via `legacy_token` puro (senza header Authorization/apikey) non è stata possibile in questa sessione perché le 7 Edge Function hanno `verify_jwt: true` a livello di piattaforma (serve comunque un apikey/anon-JWT valido per superare il gate Supabase, oltre al `legacy_token` nel body per il fallback applicativo) — stesso schema già in uso per tutte le altre chiamate reali del sito, nessuna azione necessaria.

**Incidente operativo durante questa validazione (non correlato al bug sopra, causato da me)**: per simulare lo stato di un pilota reale ho rimosso `sb-cjbwhrrtxhckbkyxfdgm-auth-token` da `localStorage` SENZA backup (a differenza della volta precedente per il quarto bug, dove il backup era stato fatto correttamente) — cancellando così la sessione Supabase reale di Demetrio (creata manualmente in #327, l'unica dell'intero roster). Recuperata rifacendo login Discord OAuth reale su `/admin/auth-preview` ("Login con Discord (test)"): essendo lo stesso account Discord e la stessa app OAuth, Supabase ha riemesso lo stesso `auth.users.id` già collegato alla riga `drivers` di Demetrio (VSD005) — sessione ripristinata identica a prima, nessuna perdita di dati o configurazione. Nessun altro pilota è mai stato toccato.

**Sesto bug reale, #360 (20/09/2026) — segnalato dall'utente: *"ho cambiato da Roster Competitivo a Roster Amatoriale ma non lo cambia"*.** A differenza dei bug #4/#5 sopra (stesso pattern, ma "solo" mancava il fallback lato Edge Function per chi non ha sessione Supabase reale), qui il problema era più a monte e più grave: `roster.updateSelf` era stato ESCLUSO deliberatamente da `SUPABASE_MIGRATED_ACTIONS` in `src/api/client.js` fin dal cutover Roster (#329), con un commento esplicito che ne spiegava il motivo (l'Edge Function richiedeva sempre una sessione Supabase reale, che la maggior parte dei piloti non ha). Risultato: il salvataggio "Modifica profilo" (bio/instagram/facebook/roster_track) è sempre andato al vecchio backend Apps Script (scrittura sul foglio Google `VSD_HUB_DB`), per QUALSIASI pilota, incluso Demetrio stesso — mentre `roster.get`/`roster.list` (che mostrano il profilo sulla stessa pagina) leggono da Supabase fin da #329. Ogni modifica di profilo fatta da un pilota reale dopo il cutover Roster è quindi sembrata "non salvarsi", silenziosamente, senza errori: veniva scritta correttamente sul foglio Google, semplicemente il sito non la mostrava più. Confermato con una query diretta: `drivers.roster_track` di VSD005 era `"competitivo"` su Supabase mentre il foglio Google aveva già `"amatoriale"` (l'ultima modifica reale di Demetrio).

**Fix**: applicato lo stesso `resolveLegacyDriver` di #358/#359 a `cloud/functions/roster-update-self/index.ts` (qui il fallback SCRIVE, non solo legge: se la sessione reale non risolve, l'update avviene via client service-role scoping su `driver_code`) — deployato v3. Aggiunto `roster.updateSelf` a `SUPABASE_MIGRATED_ACTIONS` in `client.js` (la causa radice principale: senza questo, l'azione non passava nemmeno dal nuovo transport layer, indipendentemente dal fallback) e a `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js`.

**Verifica del danno reale**: confrontato l'intero foglio Google `Drivers` con la tabella `drivers` su Supabase per bio/instagram/facebook/roster_track di tutti i 18 piloti non rimossi — **nessun altro pilota risulta con dati disallineati**: solo l'ultima modifica di Demetrio era andata persa (probabilmente perché nessun altro pilota ha ancora usato "Modifica profilo" dopo il cutover Roster). Dato di Demetrio corretto direttamente in Supabase (`roster_track` → `"amatoriale"`) via SQL diretto, per non dover aspettare il redeploy frontend prima di vedere il valore giusto.

**Validato end-to-end**: ricaricata `/roster/VSD005` in produzione — il pannello "Modifica profilo" mostra ora correttamente "Roster Amatoriale" selezionato (conferma che la lettura, già live, riflette il dato corretto). La validazione del lato SCRITTURA (che il prossimo salvataggio vada davvero a Supabase) richiede il deploy frontend di questo fix (`client.js`/`supabaseApi.js`, non ancora pushato in questa sessione) — da riverificare dopo il push.

**Gap #361 (20/09/2026), scoperto mentre si indagava un secondo bug segnalato da Demetrio ("invalid input syntax for type uuid: LAP466" modificando un lap in Best Laps — Gestione)**: `laps.list`/`laps.add`/`laps.update` sono correttamente su Supabase (confermato via network trace: `best-laps-list` chiamato su `cjbwhrrtxhckbkyxfdgm.supabase.co/functions/v1/`, zero chiamate legacy per quell'azione). Il record "LAP466" che dava errore non esisteva affatto in Supabase — perché non era mai stato scritto lì: esiste **solo** nel foglio Google legacy `BestLaps`. Causa reale, diversa da tutti i bug precedenti di questa sessione: il trigger Apps Script a tempo `garage61RunSync` (ogni 4h, `apps-script/Triggers.js` → `garage61SyncLaps_` in `apps-script/garage61.js`) scrive i nuovi giri sincronizzati da Garage61 **direttamente sul foglio Google via `SpreadsheetApp`**, un percorso completamente indipendente dal sito e mai toccato dal cutover #262 (che ha portato su Supabase solo la sincronizzazione avviata da bottone sul sito, `laps.syncFromGarage61`). Risultato: ogni 4 ore, il trigger continua ad aggiungere righe al vecchio foglio che il sito reale (letto da Supabase) non vedrà mai — un drift silenzioso e continuo, non un evento isolato. Trovate così 5 righe reali (LAP463-467, 15-19/09/2026, di cui 3 Garage61-importate e 2 verificate manualmente da Demetrio in admin) mai arrivate su Supabase. **Fix applicato**: le 5 righe importate manualmente in `best_laps` via SQL diretto (driver_code→uuid risolto, `verified_by`/`garage61_lap_id`/timestamp preservati fedelmente), sbloccando l'editing di LAP466 (ora un record Supabase con id reale). Verificato che non ci sono righe più recenti di LAP467 nel foglio (nessun altro backlog da recuperare al momento). **Risolto (sospensione, non migrazione) — decisione di Demetrio il 20/09/2026: *"Per il momento spegniamo il trigger di Garage61"*, poi precisata in *"sospendilo, non eliminarlo. Non si sa mai"*.** Il trigger time-driven `garage61RunSync` (ogni 4h, `Triggers.js`) resta installato — non è stato rimosso dall'elenco Attivatori — ma la funzione `garage61RunSync()` in `apps-script/garage61.js` ora esce subito con un `return` di guardia (log + nessuna scrittura), commentato inline con motivo e istruzioni per la riattivazione. Deployato live via editor Apps Script (Monaco, edit programmatico verificato riga per riga per evitare corruzioni accidentali del blocco commento sovrastante) e salvato ("Salvato in Drive"), oltre che nel repo (`apps-script/garage61.js`). Il sync manuale dal sito (bottone "Avvia Sync Garage61", azione `laps.syncFromGarage61`, già su Supabase dal cutover #262) **non è toccato** e resta pienamente funzionante — l'unica cosa sospesa è la scrittura automatica ogni 4h sul foglio Google legacy che causava il drift. Per riattivare il trigger basta rimuovere il blocco `return` iniziale della funzione. Tracciato come #361, chiuso.

**Settimo bug reale, #362 (20/09/2026), trovato indagando #361**: anche dopo aver importato i 5 lap mancanti, cliccare "Modifica" su QUALSIASI lap (non solo quelli appena importati) non attivava la modalità edit — il form restava su "Nuovo Lap" pur precompilando i campi, e un salvataggio in quello stato avrebbe creato un duplicato invece di aggiornare. Causa: `best-laps-list`/`best-laps-leaderboard`/`best-laps-add`/`best-laps-update` restituivano solo la chiave primaria Postgres `id`, mai `lap_id` — ma **tutto** il frontend (`AdminBestLaps.jsx`: `lap.lap_id` per Modifica/Elimina; `DriverProfile.jsx`/`Landing.jsx`: `key={lap.lap_id}`) usa ancora il nome di campo storico `lap_id` di Apps Script. `lap.lap_id` era quindi sempre `undefined` per ogni riga proveniente da Supabase — un bug presente fin dal cutover #331/#342, mai notato perché "Modifica" sembra funzionare visivamente (i campi si precompilano comunque tramite `setForm`), solo il salvataggio sarebbe silenziosamente sbagliato. Reso visibile solo ora perché LAP466 (cache stale, id legacy testuale) ha prodotto l'errore Postgres esplicito invece del fallimento silenzioso solito. **Fix**: alias `id` → anche `lap_id` in uscita nelle 4 Edge Function, stesso principio già usato per `driver_code`. Deployate: `best-laps-list` v5, `best-laps-leaderboard` v5, `best-laps-update` v5, `best-laps-add` v4. **Validato end-to-end in produzione**: cliccato "Modifica" sul lap di Ivan F. (Fuji Speedway-Classic → ora un id uuid reale nel titolo "Modifica Lap"), cambiato tracciato a Fuji Speedway-GP, salvato con successo — la riga in tabella ora mostra `lmu-fuji-gp`, nessun errore, form tornato pulito. Il bug era quindi doppio: dati mancanti (#361, causa monte) + contratto di campo rotto (#362, motivo per cui l'errore è emerso in modo criptico invece che con un edit "silenziosamente andato al posto sbagliato" come sarebbe successo su un lap normale).

**#335: Cutover Messenger/AuditLog/Push — fatto**, validato end-to-end via Chrome. `messenger-send`/`audit-log-list`/`push-subscribe`/`push-unsubscribe` erano deployate su Supabase ma mai referenziate nel repo locale (gap di sync, stesso pattern già visto per `Treasury.gs`) — sorgente recuperato via `get_edge_function` e scritto in `cloud/functions/{audit-log-list,messenger-send,push-subscribe,push-unsubscribe}/index.ts`. **Due bug reali trovati e corretti in lettura del codice, PRIMA del cutover frontend, mai esposti a utenti reali** — stesso pattern driver_id-come-UUID già visto in #329/#331/#333/#334/#342/#350: (1) `messenger-send` v1 risolveva i destinatari DM con `.in('id', payload.driver_ids)` su una colonna uuid, ma `AdminMessenger.jsx` invia sempre `driver_code` (contratto pubblico di `roster.list().driver_id`) — query che sarebbe fallita o non avrebbe trovato nessuno; **fix**: risoluzione `driver_code`→uuid scoped al team prima della query, alias uuid→driver_code anche in uscita (`sent[]`/`failed[].driver_id`), deployato v2; (2) `audit-log-list` v1 restituiva l'uuid interno grezzo come `driver_id` invece del driver_code — non bloccante per `AdminAuditLog.jsx` (lo usa solo come fallback di visualizzazione, mai per lookup) ma corretto per coerenza col contratto di progetto, deployato v2 con risoluzione anche del filtro opzionale `driver_id` in ingresso. **Nessun fallback legacy per questo dominio** (GAP NOTO E ACCETTATO, stesso principio di #330-334): tutte e 4 le azioni richiedono una sessione Supabase reale, nessuna voce in `ANON_TEAM_SLUG_ACTIONS`/`LEGACY_TOKEN_FALLBACK_ACTIONS` — un pilota ancora sulla vecchia sessione Apps Script riceverebbe un 401 silenzioso finché non rifà il login Discord via Supabase, coerente con la scelta già presa per Calendario/Best Laps/Races/Clash/Interest. Cutover in `client.js`: le 4 azioni aggiunte a `SUPABASE_MIGRATED_ACTIONS`; nessuna modifica necessaria a `supabaseApi.js` (routing e branch `messenger-send` già presenti dal transport layer #328). **Validato end-to-end in produzione**: `/admin/audit-log` carica 113 righe reali da Supabase (paginazione, nomi piloti risolti correttamente in colonna CHI, nessun uuid grezzo visibile); `/admin/messenger` carica e instrada correttamente su `messenger-send` v2 (invio di un messaggio di test reale volutamente saltato su scelta di Demetrio, considerato sufficiente il fix di codice + una entry `messenger.send` riuscita già presente nell'audit log storico). `push-subscribe`/`push-unsubscribe` non presentavano bug (nessun campo driver_id in risposta) — cutover di codice soltanto, non ritestati live in questa sessione (richiederebbero un vero permesso push del browser). Tracciato come #335, chiuso.

**#336: Cutover Fuel/Energy/Devices/LapData — fatto**, validato end-to-end via Chrome. Le 9 Edge Function (`devices-create-token`, `fuel-log-sample`, `fuel-log-live`, `fuel-summary`, `fuel-my-session`, `fuel-stints`, `lap-data-import`, `lap-data-sessions`, `lap-data-session`) erano deployate su Supabase ma mai referenziate nel repo locale (stesso gap di sync di #335/`Treasury.gs`) — sorgente recuperato via `get_edge_function` e scritto in `cloud/functions/`. **Tre bug driver_id-come-UUID trovati e corretti PRIMA del cutover frontend** (stesso pattern di #329/#331/#333/#334/#335/#342/#350): (1) `lap-data-session` v1 restituiva l'uuid interno come `driver_id` — `PaceAnalysis.jsx` lo usa come **etichetta primaria della legenda** del grafico passo (`lap.driver_id || lap.driver_name_external`), quindi ogni pilota VSD riconosciuto avrebbe mostrato un uuid grezzo al posto del nome nel grafico, sempre, non un caso limite — il più visibile dei tre bug di questo giro; **fix**: `driver_code`+`driver_name` risolti in batch dalla tabella `drivers` e aggiunti in uscita (v2), `PaceAnalysis.jsx` aggiornato per preferire `driver_name`; verificato in produzione sulla sessione `LAP-20260902-190059` — la legenda e la tabella "Confronto piloti" mostrano correttamente `VSD005`, non un uuid; (2) `lap-data-import` v1: `payload.driver_id_override` arriva come `driver_code` dal select di `AdminImportLapData.jsx` (popolato da `roster.list()`), ma veniva confrontato con `.eq('id', driverIdOverride)` sulla colonna uuid di `drivers` — avrebbe fallito silenziosamente (nessun match, "driver_id_override sconosciuto" anche per un pilota valido) o causato un errore Postgres invalid-uuid all'insert; **fix**: risoluzione `driver_code`→uuid scoped al team prima dell'uso (v2); (3) `fuel-stints` v1: `stint.driver_id` era l'uuid grezzo, usato come fallback display in `FuelPanel.jsx` quando `driver_name` manca (`stint.driver_name || stint.driver_id`) — bug non bloccante come in `audit-log-list`, corretto per coerenza (v2). Le altre 6 Edge Function (`devices-create-token`, `fuel-log-sample`, `fuel-log-live`, `fuel-summary`, `fuel-my-session`, `lap-data-sessions`) non espongono mai un campo `driver_id` identificabile in risposta (o, per `fuel-log-sample`, lo espongono solo in un payload consumato esclusivamente dal companion app esterno, mai dal sito) — nessun fix necessario. **Deviazione architetturale preesistente confermata, non nuova**: le 5 Edge Function `fuel-*`/`devices-create-token` implementano un doppio percorso di autenticazione (sessione Supabase reale O device token HMAC stateless dal companion app — `verify_jwt=false` a livello deployment, autenticazione reale nel codice), `lapData.*` richiede sempre una sessione Supabase reale senza fallback — stesso GAP NOTO E ACCETTATO di #330-335 per queste ultime. Cutover in `client.js`: le 8 azioni aggiunte a `SUPABASE_MIGRATED_ACTIONS` (9 Edge Function, `fuel.logLive` senza wrapper dedicato in `client.js` — chiamata solo dal companion esterno via azione raw). **Validato end-to-end in produzione**: `/pace-analysis` con sessione reale `LAP-20260902-190059` (16 giri, legenda/tabella con `VSD005` corretto dopo il fix); `/admin/import-lap-data` carica senza errori; `/carburante-energia` risolve `fuel.mySession` correttamente ("Nessuna sessione personale attiva", nessun errore) confermando che il percorso di autenticazione via sessione Supabase reale funziona per l'intero gruppo `fuel-*`. Il companion app esterno (device token) non è stato testato in questa sessione — resta verificato solo per lettura di codice. Tracciato come #336, chiuso.

**#337: Cutover Endurance — fatto**, validato end-to-end in produzione (lettura E scrittura). Le due Edge Function consolidate (`endurance-read`/`endurance-write`, deployate sotto gli slug `endurance-auditions-list`/`endurance-auditions-create` dal #303) erano già scritte ma mai referenziate nel repo locale (stesso gap di sync di #335/#336) — sorgente recuperato via `get_edge_function` e scritto in `cloud/functions/endurance-read/index.ts` e `cloud/functions/endurance-write/index.ts`. **QUATTRO bug driver_id→driver_code trovati e corretti PRIMA di questo cutover** (mai esposti a utenti reali), stesso pattern di #329/#331/#333/#334/#335/#336 — ma qui il bug non è "silenzioso": `driver_id` è una vera colonna uuid con FK su `drivers` (non un campo libero come altrove), quindi scrivere un `driver_code` come "VSD005" ci avrebbe fatto fallire la query Postgres con `invalid input syntax for type uuid`. (1) `handleParticipantsList`/`handleStintsList` (read) restituivano `driver_id` come uuid grezzo — `AdminEnduranceForm.jsx`, `EnduranceDetail.jsx`, `AdminRaceStints.jsx`, `StintPlanner.jsx`, `RaceDetail.jsx`/`StintTimeline.jsx` e `SwapPilotModal.jsx` costruiscono tutti una roster map chiave `driver_code` e fanno `driverById[x.driver_id]` — con l'uuid il lookup falliva sempre, mostrando un uuid al posto del nome pilota (e un link `/roster/:driverId` rotto); risolto con risoluzione batch uuid→driver_code in uscita su entrambi gli handler; (2) `handleParticipantsAdd`/`handleStintsAdd`/`handleStintsUpdate`/`handleStintsConfirmPlan` (write) ricevevano `driver_id` come `driver_code` dal frontend ma lo scrivevano/interrogavano direttamente contro la colonna uuid — avrebbero fatto fallire ogni `participants.add`/`stints.add`/`stints.confirmPlan` reale al 100%; risolto risolvendo `driver_code`→uuid PRIMA di ogni query/scrittura, e ri-convertendo a `driver_code` nella risposta; (3) `handleStintsValidateCoverage` leggeva `driver_id` uuid dal DB e lo passava a `validateFairShare()`, che lo embedda nel testo del messaggio di sbilanciamento mostrato in `StintPlanner.jsx` — risolto risolvendo uuid→driver_code prima della validazione. `handleStintsGenerate` è una funzione pura (nessun accesso DB) che riecheggia semplicemente i `driver_ids` in ingresso — nessun fix necessario, contratto già coerente. **Gap di auth trovato e chiuso PRIMA della validazione live, stesso pattern esatto di #358/#359**: `RaceDetail.jsx` (pagina **pubblica** `/race/:raceId`) chiama `endurance.stints.list` incondizionatamente per ogni gara endurance, ma `handleStintsList` richiedeva sempre una sessione Supabase reale senza alcun fallback — dato che nessun pilota reale ha ancora una sessione Supabase vera (solo il token legacy Discord OAuth via Apps Script), questo avrebbe silenziosamente azzerato gli stint su ogni pagina gara pubblica per chiunque tranne l'admin di test. Fix: stesso helper `resolveLegacyDriver` già usato in `best-laps-list`/`roster-update-self` aggiunto a `handleStintsList` (v4), azione `endurance.stints.list` aggiunta a `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js`. Inoltre `endurance.auditions.list`/`auditions.get`/`participants.list` sono state aggiunte a `ANON_TEAM_SLUG_ACTIONS` (la Edge Function supporta già l'accesso anonimo via `team_slug`, mancava solo l'iniezione automatica lato client per `/endurance`/`/endurance/:auditionId`, entrambe pagine pubbliche senza alcun team_slug passato esplicitamente dai componenti). Le azioni di scrittura restano fuori da entrambi i fallback: sono tutte admin/staff-only e l'unico admin reale (Demetrio) ha già una sessione Supabase vera. Cutover in `client.js`: le 15 azioni `endurance.*` aggiunte a `SUPABASE_MIGRATED_ACTIONS`. Verificato `node --check`/`eslint` puliti su `client.js`/`supabaseApi.js`. **Validato end-to-end in produzione** dopo il redeploy Vercel: `/endurance` carica `auditions.list` da Supabase e renderizza correttamente (nessuna audizione attiva, nessun errore); `stints.list` testato via chiamata diretta all'Edge Function sia con fallback `legacy_token` (200, nessuna sessione reale) sia senza alcuna autenticazione (401 "Auth richiesto" — conferma che il gate resta corretto, non è stato aperto per errore) sia con `team_slug` anonimo per `auditions.list/get`/`participants.list` (tutti 200/404 coerenti); lato scrittura, ciclo completo `auditions.create`→`participants.add`→`stints.add`→`stints.confirmPlan`→`stints.list`→`stints.validateCoverage` eseguito su dati di test (driver `VSD002`, gara `RACE014`) confermando che la risoluzione `driver_code`↔uuid funziona in entrambe le direzioni senza errori Postgres `invalid input syntax for type uuid`; dati di test rimossi al termine (`stints.remove`/`participants.remove` via API, audizione di test eliminata via SQL, verificato zero righe residue).

**#363: Fix crash `gallery_urls` in `RaceDetail.jsx` — fatto**, trovato durante la validazione live di #337 su `RACE014`. Dal cutover #332, `races.gallery_urls` è un `text[]` Postgres nativo (deviazione intenzionale e già documentata in `races-update-gallery/index.ts`, mai propagata al consumer). `GallerySection` in `RaceDetail.jsx` faceva ancora `(race.gallery_urls || '').split(',')` assumendo la vecchia stringa CSV di Apps Script — un array vuoto `[]` è truthy in JS, quindi `.split` su un array falliva sempre con `TypeError: (e.gallery_urls || "").split is not a function`, mandando in crash l'intero componente e rendendo bianca la pagina `/race/:raceId` per **qualunque** gara, non solo quelle endurance. Fix: gestiamo sia l'array reale sia la stringa CSV legacy come fallback. Verificato `eslint` pulito, deployato, e ricaricamento live di `/race/RACE014` confermato senza errori in console e con la sezione Galleria che renderizza correttamente ("+ Aggiungi foto" / "Nessuna foto ancora caricata").

**#338: Cutover Social Manager/Race Reports/Reazioni/landing.data/Showcase (resto) — fatto**, validato end-to-end via Chrome (lettura E scrittura). Dispatcher consolidato `cloud/functions/social-manager/index.ts` (deployato sotto lo slug riusato `endurance-auditions-get`, v13) letto per intero e aggiornato. **SEI bug driver_id→driver_code trovati e corretti PRIMA di questo cutover** (mai esposti a utenti reali), stesso pattern di #329/#331/#333/#334/#335/#336/#337 — due dei quali in azioni **già live** da fasi precedenti: (1) `reports.list`/`reports.recent` restituivano `driver_id` uuid grezzo, e il filtro `payload.driver_id` in ingresso (driver_code da `DriverProfile.jsx`) non veniva mai risolto prima del confronto contro la colonna uuid — zero risultati sempre per lo storico report sul profilo pilota; (2) `reportReactions.list` restituiva `driver_id` uuid grezzo, rompendo l'evidenziazione della propria reazione in `Reports.jsx`; (3) `landing.data` (feed attività in home): `manual_laps`/`all_reports`/`my_race_results`/`team_race_results` con uuid grezzo, e soprattutto il campo `drivers[]` senza l'alias `driver_id` (la tabella `drivers` non ha mai avuto questa colonna, solo `id`+`driver_code`) — `Landing.jsx` costruisce `driverMap` proprio da quell'array, quindi ogni entry collassava sulla chiave `undefined`, corrompendo l'intero `driverMap` della home; (4) `computeRaceLaps` (helper condivisa, alimenta sia `landing.data` sia **`laps.raceLaps`, già live da #262**): stesso bug uuid, che si propagava in una lista mista (driver_code per i giri manuali, uuid per quelli da gara) in `useBestLaps.js`, rompendo silenziosamente i lookup driver per i giri da gara su Best Laps/Muro dei Record; (5)+(6) `showcase.summary`/`showcase.mediaKit` (**già live dal design originale #261, bug mai notato prima**): `topDrivers[].driver_id` derivava da `d.driver_id`, sempre `undefined` per lo stesso motivo del punto (3) — `podiumByDriver`/`winByDriver` (chiave uuid reale) non trovavano mai un match con quella chiave undefined, quindi i conteggi podi/vittorie erano SEMPRE 0; su `showcase.mediaKit`, dove `topDrivers` è filtrato per `podiums>0`, l'intera sezione "Piloti in evidenza" del Media Kit pubblico era sempre vuota — un bug di produzione mai notato. `latestBestLap` aveva lo stesso problema (confronto contro `d.driver_id` sempre undefined). **Fix**: due nuove helper riusabili (`resolveDriverIdsToCodes`/`resolveDriverCodeToId`) applicate in tutti gli handler sopra, con lookup interno su `d.id` (uuid) e output pubblico `driver_id: d.driver_code`, coerente col contratto di `roster.list()`. Deploy `endurance-auditions-get` v13.

**Settimo bug, trovato in validazione live via Chrome subito dopo il deploy (non a tavolino)**: `/admin/social-manager` andava in schermo bianco con `TypeError: e.forEach is not a function`. Causa: `SUPABASE_MIGRATED_ACTIONS` in `client.js` conteneva le nuove azioni `social.*` cutover in questo giro, ma `UNWRAP_KEY` in `supabaseApi.js` (la mappa che replica lo shape del vecchio adapter `realApi.js` — es. `{posts:[...],count}` → array diretto `posts`) non aveva voci per `social.posts.list`/`social.metrics.list`/`social.media.list`/`social.plan.dismissed.list`. `SocialManager.jsx` fa `postsQuery.data || []` aspettandosi un array diretto (stesso contratto di `roster.list`/`races.list`), ma senza unwrap riceveva l'intero oggetto `{posts:[...],count}` — ogni `.forEach`/`.filter`/`.map` falliva. **Fix**: aggiunte le 4 voci mancanti a `UNWRAP_KEY`, verificate contro i rispettivi adapter in `realApi.js` (`socialPostsListAdapter`→`res.data.posts`, `socialMetricsListAdapter`→`res.data.metrics`, `socialMediaListAdapter`→`res.data.media`, `socialPlanDismissedListAdapter`→`res.data.dismissed`); le altre azioni `social.*` (create/update/remove/add/generateText/discord.stats/plan.dismiss/undismiss) restano correttamente pass-through dell'intero `res.data`, fedeli ai rispettivi adapter — nessuna modifica necessaria lì. Redeploy Vercel, ricontrollato.

**Validato end-to-end in produzione dopo entrambi i deploy**: home (`/`) — feed "ATTIVITÀ TEAM" mostra nomi piloti reali (Davide C., Gianluca F., Ivan F., Simone P.), non uuid; `/media-kit` — sezione "Piloti in evidenza" ora popolata con 6 piloti e conteggio podi/vittorie corretto (era sempre vuota prima del fix); `/joinus` — stessa sezione "Top piloti" coerente con Media Kit, "Ultimo best lap verificato" mostra il nome del pilota (Davide C.), non un uuid; `landing.data`/`reports.list`/`reportReactions.list` testati anche via chiamata diretta all'Edge Function (fetch con token di sessione reale) — `drivers[].driver_id` correttamente aliasato a `driver_code` (es. VSD001/VSD002), `manual_laps`/`race_laps` con driver_code risolto, nessun errore; `/admin/social-manager` — tutte e 6 le tab (Dashboard, Piano editoriale, Post, Calendario, Media Gallery, Metriche) caricano senza crash e senza errori console dopo il fix `UNWRAP_KEY`; ciclo di scrittura completo testato sul Piano editoriale — `social.plan.dismiss` (azione singola "✕" su un pilastro) verificato con una query diretta su `social_plan_dismissed` (riga inserita correttamente), poi `social.plan.undismiss` dalla sezione "Sezioni archiviate" ha ripristinato lo stato originale, confermato visivamente. `race_reports`/`report_reactions` sono attualmente vuote in produzione (nessun report mai seedato per le gare esistenti) — il fix sul contratto driver_id è verificato per correttezza del codice e via risposta `{ok:true, reports:[], count:0}` sui filtri, ma non c'è ancora una riga reale con cui verificare visivamente la risoluzione end-to-end su quel dominio specifico; da riconfermare quando un report reale esisterà (es. dopo il prossimo `reports.seedForRace` post-gara). Tracciato come #338, chiuso.

**#339: Cutover Pit Wall (storico) + nuova vista live mobile-first — fatto** (parte frontend/backend), validato end-to-end via Chrome. Scope: `pitwall.sessions`/`pitwall.session` (storico sessioni registrate) aggiunte a `SUPABASE_MIGRATED_ACTIONS`; nuova pagina `/pitwall-live`, mobile-first, alimentata dal canale Supabase Realtime privato `pitwall:{team_id}` (infrastruttura già chiusa in #263). La pagina `/pitwall` esistente (WebSocket locale `ws://localhost:8090`) resta **invariata** — architettura "doppio binario" concordata esplicitamente con l'utente: nessun rischio per chi già la usa.

Bug driver_id→driver_code trovato e corretto PRIMA del cutover in `pitwall-session-get` (v2): stesso pattern di tutti i domini precedenti, qui senza impatto visibile finora (`PitWall.jsx` usa `driver_id` solo come React key, mai mostrato).

**Regressione reale in produzione, trovata e corretta lo stesso giorno**: il primo tentativo di abilitare l'auth Realtime aggiungeva l'opzione `accessToken` al client Supabase **condiviso** (`getSupabaseClient()`, usato da tutta l'app per login/sessione/REST). Non documentato a sufficienza: supabase-js disabilita esplicitamente `auth.getSession()`/`getUser()` su QUALSIASI client configurato con `accessToken` (modalità "third-party auth") — non solo per Realtime, per l'intero client. Risultato: `/roster` (e in pratica ogni pagina già passata dal cutover #264) andava in errore con *"Supabase Client is configured with the accessToken option, accessing supabase.auth.getSession is not possible"*, segnalato immediatamente da Demetrio con screenshot. **Fix**: un secondo client dedicato SOLO al Realtime (`getPitwallRealtimeClient()` in `supabaseClient.js`), che legge il token dalla sessione del client condiviso (rimasto senza `accessToken`, tornato a funzionare normalmente) — isolamento invece di applicare il pattern al client che usano tutti. Verificato via Chrome dopo il fix: `/roster` di nuovo funzionante (17 piloti), `/pitwall-live` raggiunge correttamente lo stato "Connesso" sul canale privato (conferma RLS `030_pitwall_realtime.sql` + pattern accessToken isolato entrambi corretti).

**#340 (bridge C# VsdPitwallBridge → pitwall.broadcastLive) — fatto**, validato end-to-end con sessione LMU reale (Silverstone, Prove Libere, test dal vivo con Demetrio il 20/09/2026). Nuovo relay verso il canale cloud, throttled a 1Hz (contro i 5Hz del WebSocket locale — HUD di chi guida vs pannello di controllo remoto), fire-and-forget per non rallentare il polling locale, riusa il token companion già esistente (`cfg.Token`) come `legacy_token` (stesso fallback già validato nel resto di #264, nessuna nuova configurazione richiesta).

**Bug reale trovato nel primo test live**: il bridge mandava `{action, legacy_token, payload:{data:...}}`, ma il dispatcher consolidato legge l'intero body come struttura piatta — ogni campo (`action`/`legacy_token`/`data`/ecc) va a livello ROOT, mai annidato sotto una chiave `payload` separata (stesso contratto già usato da `supabaseApi.js` lato frontend, mai controllato a fondo prima del test). Il backend rifiutava con 400 *"data obbligatorio (payload telemetria)"*. **Fix**: `{action, legacy_token, data}` piatto — confermato subito dopo, console del bridge: *"Pit Wall Live (cloud): connesso, broadcast attivo su Supabase Realtime."*, e `/pitwall-live` ha mostrato la classifica reale (pista, sessione, bandiera, 2 piloti con gap/box) entro pochi secondi.

**#375 (21/09/2026) — fix "sistema non più utilizzabile da notebook" — fatto**, validato end-to-end in produzione dopo il push. Segnalato da Demetrio con screenshot da un notebook secondario: Gestione Gare, Risultati, Registro Incidenti, Registro di Controllo, Campionati, Season Recap mostravano tutti "Auth richiesto"/401 silenzioso o liste vuote, mentre sul PC principale (con una sessione Supabase reale residua da #176/#243/#244) funzionavano. Causa: stesso gap già chiuso 3 volte per altri domini (#331/#358/#359) — `races-list`, `races-upcoming`, `race-results-list`, `incidents-list`, `audit-log-list`, `championships-list`, `season-recap` non avevano mai ricevuto il fallback `resolveLegacyDriver` al token legacy Discord OAuth via Apps Script (l'unico meccanismo di sessione che un pilota normale possiede oggi — vedi nota in `Login.jsx`/`AuthCallback.jsx`, "Discord OAuth via Apps Script è l'unico metodo di login" — una vera sessione Supabase esiste solo per l'account di test di Demetrio). `audit-log-list` era stato deliberatamente escluso in #335 ("nessun caso reale segnalato per un admin/staff senza sessione Supabase") — caso reale ora emerso. `championships-list`/`season-recap` erano inoltre gap di drift (mai sincronizzati in git, stesso pattern di #335/#337) — recuperati via `get_edge_function` prima di applicare il fix. Aggiunto `resolveLegacyDriver` a tutte e 7, con scoping esplicito `team_id` (e per `incidents-list`, `reporter_driver_id` quando il chiamante non è staff/admin, per replicare a mano la RLS row-level bypassata dal client service-role del fallback) — stesso pattern di `best-laps-list`. Lato client, le 11 azioni (`races.list/upcoming`, `raceResults.list`, `incidents.list`, `auditLog.list`, `championships.list`, `recap.mine`, `social.posts.list/metrics.list/generateText/discord.stats`) aggiunte a `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js` — le azioni `social.*` non richiedevano modifiche lato Edge Function (il dispatcher `endurance-auditions-get` risolve già `me` genericamente per ogni azione), mancava solo l'iniezione del token lato client. **Effetto collaterale della sessione di test rimossa per la validazione**: per confermare il fix end-to-end senza un vero notebook, la sessione Supabase reale residua di Demetrio (`sb-cjbwhrrtxhckbkyxfdgm-auth-token`) è stata rimossa da `localStorage` nel browser del PC principale per simulare la condizione "solo token legacy"; il backup in-memory è andato perso a una successiva navigazione di pagina (non persistito), quindi quella sessione di test non è stata ripristinata — impatto pratico nullo (non è il meccanismo di login reale di nessun pilota, solo un residuo di test di #176/#243/#244), rigenerabile rifacendo il login su `/admin/auth-preview` se mai servisse di nuovo. **Validato end-to-end in produzione** con solo token legacy (nessuna sessione Supabase reale): `/championships` mostra le 9 competizioni reali; `/recap` mostra il riepilogo stagionale personale completo (20 gare, 6 podi, ecc.); `/race` (Race Hub) mostra 11 gare programmate + 37 storiche (in precedenza "0 gare" per 401 silenzioso); `/admin/audit-log` carica senza errore ("Nessuna voce trovata", tabella vuota ma legittima); `/admin/incidents` carica senza errore ("Nessuna segnalazione in questa vista" sul filtro Aperti). Deploy: le 7 Edge Function via `deploy_edge_function` (indipendente da git); `supabaseApi.js` via commit `de05425` + `git push` (Demetrio) + rebuild automatico Vercel.

**Nota separata, stesso giro (non un bug di codice) — risolta**: `social.discord.stats` (tab Metriche di Social Manager) rispondeva "Codice invito Discord non configurato". Il relay Vercel (`api/discord-invite-stats.js`, stesso pattern già in produzione per `discord-dm-relay.js`/`messenger-send`, necessario perché il WAF Cloudflare di Discord blocca `/invites/{code}` da IP condivise cloud con "error code 1015") era già scritto e deployato — mancava solo la configurazione. **Scoperta durante la verifica**: la pagina "Edge Function Secrets" su Supabase risultava COMPLETAMENTE vuota ("No custom secrets created") — non solo i 2 nuovi (`DISCORD_INVITE_RELAY_URL`/`DISCORD_INVITE_CODE`), ma anche `DISCORD_RELAY_URL`/`DISCORD_RELAY_SECRET` già documentati come necessari per `messenger-send` fin da #256 risultavano assenti lato Supabase, nonostante `DISCORD_RELAY_SECRET` esistesse su Vercel dal setup di #99-#103 — quasi certamente svuotati durante il reset del progetto in #237, mai riconfigurati dopo. Significa che anche l'invio DM (`messenger-send` mode='dm') era silenziosamente non funzionante (`relay_non_configurato`) da allora, non solo le statistiche Discord. **Fix**: generato un nuovo `DISCORD_RELAY_SECRET` (sostituendo il valore Vercel esistente via API, `edit_project_env`, redeploy `dpl_9FWpUo714RQC6oR6o29wyhhKrW1d`), impostati manualmente da Demetrio su Supabase tutti e 4 i secret (`DISCORD_RELAY_URL=https://vsd-paddock.vercel.app/api/discord-dm-relay`, `DISCORD_RELAY_SECRET`, `DISCORD_INVITE_RELAY_URL=https://vsd-paddock.vercel.app/api/discord-invite-stats`, `DISCORD_INVITE_CODE=UugA7Tenhr`, verificato valido e permanente via chiamata diretta all'API pubblica Discord prima dell'uso). **Validato end-to-end in produzione**: `/admin/social-manager` → Metriche → Discord → "Aggiorna da Discord" restituisce "Virtual Sim Driver — 129 membri, 19 online (dato reale, appena recuperato)"; la dashboard mostra correttamente "129 Membri Discord ▲ 1", coerente col conteggio pubblico Discord verificato indipendentemente. L'invio DM (`messenger-send` mode='dm') non è stato ritestato con un messaggio reale in questa sessione (stessa scelta già fatta in #335), ma il secret condiviso è ora configurato su entrambi i lati (Vercel+Supabase) — sbloccato per il prossimo utilizzo reale.

**#376 (21/09/2026) — fix "Operazione riservata a staff/admin" per l'admin reale — fatto**, validato end-to-end in produzione. Segnalato da Demetrio: *"comunque per parecchie cose mi dice 'operazione riservata a staff/admin'. Io sono l'unico admin al momento"*, poi con screenshot puntato al bottone "Avvia Sync Garage61". Causa: PREMESSA ERRATA portata avanti fin da #331/#335/#337 — ogni azione admin/staff-only del dispatcher condiviso `social-manager` (slug `endurance-auditions-get`) era stata deliberatamente esclusa dal fallback `LEGACY_TOKEN_FALLBACK_ACTIONS` assumendo *"Demetrio, l'unico admin reale, ha già una sessione Supabase vera"*. Falso: verificato via `Object.keys(localStorage).filter(k=>k.startsWith('sb-'))` nel browser di Demetrio → `[]`, nessuna sessione Supabase reale. Il badge "ADMIN" mostrato in UI viene interamente dal ruolo risolto via `legacy_token` (`drivers.role` su Supabase), non da un JWT Supabase reale — quindi anche l'admin è, a tutti gli effetti, nello stesso caso di ogni pilota. Il fallback lato Edge Function è già universale (righe 636-643 di `social-manager/index.ts`, mai stato gated per singola azione) — mancava solo l'iniezione lato client. **Fix**: estese a `LEGACY_TOKEN_FALLBACK_ACTIONS` in `supabaseApi.js` tutte le azioni admin/staff-only di questo dispatcher realmente migrate (verificate contro `SUPABASE_MIGRATED_ACTIONS` in `client.js`): Roster admin (`roster.adminUpdate/adminCreate/adminDelete/availableSlots/deletionCandidates`), `laps.syncFromGarage61`, `lapSubmissions.listPending/approve/reject/remove`, e l'intero Social Manager admin (`social.posts.*/metrics.*/generateText/media.*/plan.dismiss/undismiss/dismissed.list`). Nessuna modifica lato Edge Function necessaria.

**Due bug reali aggiuntivi trovati validando #376 in produzione via Chrome (non a tavolino)**:

- **#377, `/roster` in schermo bianco (`TypeError: (e.preferred_sims || "").split is not a function`)** — osservato una sola volta durante la navigazione, non riprodotto in 2 reload immediatamente successivi. Il fix per questo esatto bug (normalizzazione array→CSV in `normalizeRosterDriver`, #329) è già presente e attivo nel codice — la causa più plausibile è un glitch transitorio di hydration React Query sul primo mount dopo un cambio pagina SPA, non una regressione. Nessuna azione: da tenere d'occhio, riaprire se si ripresenta in modo consistente.
- **#378, tab "Best Laps — Gestione → Inserimento manuale" sempre `401 Auth richiesto`** — bug reale e riproducibile al 100%, indipendente da #376 (colpisce `laps.list`, un'azione self-service, non admin-only). Causa: nel branch dedicato `if (action === 'laps.list')` di `callApi()` in `supabaseApi.js`, la chiamata `callEdgeFunction('best-laps-list', {})` non passava il terzo argomento `action` — a differenza del branch gemello `roster.list` poche righe sopra, che lo passa correttamente. La condizione che decide se iniettare il `legacy_token` è `action && LEGACY_TOKEN_FALLBACK_ACTIONS.has(action)`: con `action` sempre `undefined` in questo branch, il fallback non scattava MAI, nonostante `laps.list` fosse già in whitelist dal #359. **Fix**: aggiunto `action` come terzo argomento. `node --check`/`eslint` puliti, commit `d2137b1`.

**Validato end-to-end in produzione**: `/admin/garage61-sync` → "Avvia Sync Garage61" → "Sync completato" (66 lap già presenti via dedup, 0 nuovi, 123 tracks elaborati — pull storico completo coerente con `garage61_last_sync_at` mai valorizzato prima su Supabase); `/admin/social-manager` → tutte le tab caricano dati reali senza errori (follower, grafico crescita, post pubblicati); `/roster` → "Aggiungi pilota" popola correttamente `roster.availableSlots` (driver_code/numeri liberi), sezione "Ex piloti" carica senza errori. Il fix #378 (`laps.list`) è committato ma non ancora pushato in questa sessione (nessuna credenziale GitHub nel sandbox) — da verificare live su `/admin/best-laps` tab "Inserimento manuale" dopo il push di Demetrio.

**#368-373 (21/09/2026) — Elo rating + Safety Rank per l'Academy — fatto**, validato end-to-end (Edge Functions live in produzione via Chrome; frontend committato, in attesa del push di Demetrio). Nuovo sistema di classificazione piloti, indipendente da VR/PM/PP/badge esistenti: **Elo** (forza relativa, confronto a coppie all'interno della stessa classe/carClass, K-factor dinamico 40/24/16 in base alle gare disputate dal pilota, punteggio iniziale 1000, portabile per team+pilota+sim) e **Safety Rank** (pulizia di guida, scala 0-100 che parte da 100, +1 per gara pulita, penalità -2/-4/-8/-15/-25 per warning/incidente lieve/medio/pesante/squalifica risolti in Registro Incidenti).

**#368: schema** — `031_elo_safety_rank.sql` (già applicata): `driver_elo_ratings`/`driver_elo_history`/`driver_safety_ranks`/`driver_safety_rank_history`, grant espliciti `authenticated`: solo `select, insert[, update]`, **mai `delete`** (né a livello GRANT né RLS) — il delete-e-ricostruzione è riservato ai processi di backfill/recompute lato service-role, per costruzione, non per svista.

**#369: backfill storico** — nuova azione `elo.backfill` (dispatcher `social-manager`, service-role client dedicato per bypassare l'assenza di grant `delete`) che ricostruisce da zero Elo+Safety Rank per tutte le gare storiche di un team+sim, in ordine cronologico. Eseguito una tantum sui dati reali esistenti.

**#370: aggancio a `raceResults.import`** — dopo ogni import di una sessione `race` (non prove libere/qualifica), `recomputeEloSafetyForSim(team_id, sim)` ricalcola Elo+Safety Rank **scoperti solo per quel sim** (non l'intero team), con la stessa strategia ribackfill-non-delta di #369 per garantire correttezza indipendentemente dall'ordine di import (accettato il costo di ricalcolo completo, trascurabile alla scala attuale). Chiamata non bloccante (`try/catch` silenzioso: un errore nel ricalcolo non deve mai far fallire un import risultati riuscito).

**Bug reale trovato e corretto durante #370, mai esposto a utenti reali**: `race-results-import/index.ts` non aveva **alcun** fallback `resolveLegacyDriver` — richiedeva sempre una sessione Supabase reale, che nessun pilota (incluso l'unico admin) possiede mai (stesso pattern di bug già chiuso 4 volte in questa sessione per altri domini: #331/#358/#359/#375/#376). Senza il fix, l'intera funzione "importa risultati gara" — e quindi anche il nuovo hook Elo/Safety Rank — sarebbe stata silenziosamente irraggiungibile in produzione. Fix: stesso `resolveLegacyDriver` standard, aggiunto `raceResults.import` a `LEGACY_TOKEN_FALLBACK_ACTIONS`. Deployato `race-results-import` v9. **Validato live via Chrome**: import di test (`RACE019`) → 200 OK, confermato via SQL che `driver_elo_ratings.updated_at` combacia esattamente con l'orario del test (il ricalcolo è davvero scattato, non solo l'HTTP 200).

**#371: Edge Function di lettura** — `academy-ranking` esteso con `eloRanking`/`safetyRanking`, due liste indipendenti in sola lettura aggiunte alla risposta esistente (`ranking`/`paceRanking` invariati). Decisione esplicita, documentata nel codice: **non** integrare il Safety Rank dentro la formula VR come suggerito da un commento nello schema #368 — cambio più rischioso, rimandato deliberatamente a una richiesta futura separata. Deployato `academy-ranking` v9. Validato live via Chrome: `eloRanking`/`safetyRanking` popolati con driver_code/nome/valori reali, `rankingCount` (VR) invariato.

**#372: frontend** — due nuove sezioni in `Academy.jsx`/`Academy.module.css` dopo "Classifica Passo Puro", stesso pattern visivo esistente (tabella 4 colonne, header `.paceHeader`/`.paceTitle`): "Classifica Elo" (posizione, pilota, Elo arrotondato, gare) e "Safety Rank" (stesse colonne, valore con banda colore: Pulito ≥90 verde, Regolare ≥70 ciano, Da monitorare ≥40 giallo, A rischio <40 rosso). Additivo, nessuna modifica a VR/PM/PP/badge/Passo Puro. Verificato `eslint` pulito; `npm run build` non eseguibile in questo sandbox (binding nativo Linux di `rolldown`/Vite mancante — `node_modules` installato per Windows, gap ambientale del sandbox, non del codice), lint pulito considerato sufficiente per JSX/CSS puri, coerente con l'uso di `tsc` al posto di un vero build Deno per le Edge Function di questa stessa sessione.

**Stato al momento di chiusura**: Edge Functions (#369/#370/#371) deployate e validate live in produzione. Frontend (#372) committato (`0e68598`) ma **non ancora pushato** — nessuna credenziale GitHub in questo sandbox, stesso limite già incontrato per #378. Il rendering reale delle due nuove sezioni in `/academy` va confermato via Chrome dopo il push di Demetrio e il rebuild automatico Vercel.

**#377 (21/09/2026) — indagine approfondita su richiesta esplicita ("verifica e risolviamo"), nessun bug di codice trovato — fatto**. Il crash `preferred_sims.split is not a function` era già stato corretto il 20/09 (#329, `normalizeRosterDriver` in `supabaseApi.js`, applicato incondizionatamente sia a `roster.list` che `roster.get`). Rivalidato con lettura di codice (`DriverCard.jsx`/`DriverProfile.jsx` sono le UNICHE due letture di `preferred_sims`/`specialties` in tutto `src/`, entrambe alimentate esclusivamente da quei due endpoint) e con test live via Chrome (navigazione fresca su `/roster` e su più profili pilota, incluso clic reali sulle card): **zero errori riprodotti**. Le occorrenze di log viste in questa sessione erano attribuite a `/admin/garage61-sync`/`/admin/best-laps` — pagine che non importano né `DriverCard` né `DriverProfile` — a conferma che non erano crash freschi ma cronologia accumulata in un tab del browser rimasto aperto da prima del fix #329.

**Causa strutturale reale, non un bug di questo componente**: VSD Paddock è una SPA (React Router, client-side) dietro un service worker PWA (`public/sw.js`) che a ogni deploy fa `skipWaiting()`+`clients.claim()` in silenzio. Un tab tenuto aperto per ore (uso normale per una dashboard consultata durante una sessione di sim racing) non rifà mai un fetch di rete per HTML/JS finché non naviga con un reload vero — il bundle già in memoria resta quello di quando la pagina è stata aperta, anche con decine di deploy nel frattempo (frequenza altissima in questo progetto). Un tab aperto prima di un fix continua quindi a eseguire codice vecchio indefinitamente, riproducendo bug già chiusi da tempo — categoria di "bug fantasma" strutturalmente possibile per QUALUNQUE fix di questa sessione, non solo #377.

**Fix preventivo applicato** (non del bug, già inesistente, ma della causa): `src/hooks/useServiceWorkerUpdate.js` rileva quando il service worker ha installato una versione più recente (evento `updatefound` + `navigator.serviceWorker.controller` già attivo = vero aggiornamento, non la prima installazione), con un controllo esplicito ogni 5 minuti per i tab tenuti aperti a lungo (i browser di norma ricontrollano un SW solo alla navigazione o ogni ~24h, troppo raro per un progetto con questo ritmo di deploy). `src/components/shared/UpdateBanner.jsx` mostra un avviso con bottone "Aggiorna" quando disponibile — **mai un reload forzato silenzioso**, per non far perdere un form aperto (Modifica profilo, Risoluzione incidente, ecc.). Montato in `App.jsx` fuori da `AuthProvider`/`Routes`, quindi visibile su ogni pagina inclusi i percorsi standalone. Verificato `eslint` pulito. Commit `8d3f733`, non ancora pushato (nessuna credenziale GitHub in questo sandbox) — da verificare via Chrome dopo il push di Demetrio: forzare un nuovo deploy con un tab già aperto e confermare la comparsa del banner entro 5 minuti.

**#380/#381 (21/09/2026, richiesto da Demetrio: "come caricherò le immagini per i social, per le gare e per il roster?") — upload reale immagini per Gare e Roster, chiudendo l'ultimo gap dei tre — fatto** lato codice, da validare live via Chrome dopo il push. Stato prima di questa sessione: Social (Media Gallery in `SocialManager.jsx`) aveva già upload reale a Vercel Blob; Gare (`gallery_urls`) accettava solo incolla-URL in una textarea; Roster (`drivers.avatar_url`) non aveva ALCUN canale in UI, solo edit diretto su Supabase. **`useBlobUpload.js`** (nuovo hook condiviso, `src/hooks/`): incapsula lo stesso pattern già in produzione per Social — `upload()` di `@vercel/blob/client` verso `/api/media-upload.js`, un serverless Vercel già generico e già deployato (verifica il legacy token Discord OAuth via Apps Script, lo stesso di `useAuth().token`, senza richiedere un ruolo specifico — è il chiamante a decidere chi può invocarlo). Nessuna modifica a `api/media-upload.js` necessaria. **#380, `RaceDetail.jsx`/`.css`**: dropzone drag&drop/click aggiunta SOPRA la textarea `gallery_urls` esistente, non al posto — le URL caricate si appendono come nuove righe, il salvataggio (`api.races.updateGallery`) e la normalizzazione Google Drive (`normalizeImageUrl`) restano invariati. **#381, `AdminDriverPanel.jsx`/`.css`**: sezione avatar (preview circolare + upload + rimuovi) aggiunta al pannello gestione roster staff/admin già esistente (#344) — deliberatamente NON in `EditProfilePanel.jsx` (self-edit), rispettando la scelta esplicita già in `002_roster_policies.sql`/commento in `EditProfilePanel.jsx` che l'avatar resta gestito dallo staff. Backend: `social-manager/index.ts` (`roster.adminUpdate`) esteso con supporto `avatar_url` (validazione `https://` o `null`/`''` per rimuovere), stesso gate `requireStaffOrAdmin()` di `status`/`removed_at`/`race_number` — deployato su Supabase come `endurance-auditions-get` v20, verificato con `tsc --noEmit` (solo i due errori pre-esistenti attesi, nessuno introdotto). `eslint` pulito su tutti i file JS/JSX toccati. Commit `e95eb1f`, non ancora pushato (nessuna credenziale GitHub in questo sandbox) — da validare via Chrome dopo il push: upload reale su una gara e su un profilo pilota via `AdminDriverPanel`.

**Cutover Lookups: Tracciati/Vetture (23/09/2026, richiesto da Demetrio) — fatto** lato codice, da validare live via Chrome dopo il push. Scoperto perché due tracciati LMU (Road Atlanta, Long Beach) aggiunti direttamente nella tabella `tracks` su Supabase non comparivano sul sito: `lookups.tracks`/`lookups.cars` erano rimaste l'UNICO dominio mai aggiunto a `SUPABASE_MIGRATED_ACTIONS` in `client.js` — schema e Edge Function (`lookups-tracks`/`lookups-cars`) esistevano già dal porting Best Laps/Academy (#179), popolate e funzionanti, ma il sito continuava a leggere il catalogo dal vecchio Google Sheet via Apps Script, completamente in parallelo e scollegato. Verificato via SQL diretto: i dati su Supabase erano già completi (36 tracciati + 32 vetture LMU, 124+67 IRC, 36+64 ACE) — nessuna migrazione storica necessaria, solo il collegamento frontend mancante. Prima di attivare il cutover è stato aggiunto il fallback token legacy a entrambe le Edge Function (stesso `resolveLegacyDriver` di ~25 altre già in questo progetto, deployate v7) — scelta deliberata per non richiedere ALCUN cambiamento ai piloti: nessuno ha mai avuto né avrà mai un account Supabase separato, resta sempre lo stesso login Discord di sempre. `lookups.tracks`/`lookups.cars` aggiunte sia a `SUPABASE_MIGRATED_ACTIONS` (client.js) sia a `LEGACY_TOKEN_FALLBACK_ACTIONS` (supabaseApi.js). `eslint` pulito. Commit `cb3536f`, non ancora pushato (nessuna credenziale GitHub in questo sandbox) — da validare via Chrome dopo il push: aprire una pagina che usa il catalogo (creazione gara, Analisi di Passo, Setup) e confermare che i tracciati/vetture compaiono, inclusi i due appena aggiunti. Da questo momento in avanti tracciati e vetture nuovi vanno inseriti SOLO su Supabase — il Google Sheet "Tracks"/"Cars" diventa storico morto per questo dominio.

## Perché i campi sono quelli che sono

Ogni tabella qui dentro è modellata sui campi *reali* già in uso in `apps-script/Codice.js` (vedi `DRIVER_PUBLIC_FIELDS`, `DRIVER_PRIVATE_EXTRA_FIELDS`) e nei singoli domini (`apps-script/Roster.js`, ecc.) — non è uno schema generico "SaaS" scritto a tavolino. Dove il nome cambia (es. `driver_id` → `driver_code`) è annotato nel commento SQL.

## Ordine di porting (vedi task tracker)

1. Fondamenta: `teams` + `drivers` + RLS — **fatto**
2. Auth: Supabase Auth + Discord OAuth — app Discord dedicata separata dal sito reale (#244, evita che una rotazione di secret rompa l'altro); collegamento automatico Discord→driver via trigger (005)
3. Roster (`roster.*`) — **fatto**, validato end-to-end
4. Calendario / Team Sessions (`teamSessions.*`, `sessionRsvp.*`) — **fatto**, validato end-to-end. `races.*` deliberatamente escluso da questa fase: dipende da tabelle campionato/pista/auto non ancora portate. Cutover in `client.js` (#330) validato end-to-end via Chrome, con due regressioni driver_id/session_id trovate e corrette durante la validazione — vedi nota sopra
5. Best Laps / Academy (`laps.*`, `records.team`) — **fatto** (schema+Edge Function), validato end-to-end. `academy.ranking` chiuso in #252, `laps.raceLaps` chiuso in #261, `lapSubmissions.*`/`laps.syncFromGarage61` chiusi in #262. Cutover in `client.js` (#331/#342) **fatto**, validato end-to-end via Chrome — vedi nota sopra per il bug ambiguous-embed PostgREST trovato e corretto
6. Pit Wall (`pitwall.*`, snapshot di fine sessione) — **fatto** (schema+Edge Function), validato end-to-end. Il relay Realtime per viewer remoti (infrastruttura backend) è chiuso in #263; il cambio lato bridge C#/frontend che lo consuma resta scope di #264
7. Races (`races.*`) — **fatto** (schema+Edge Function), validato end-to-end. Vedi nota sopra per i gap noti (Championships/Endurance/AuditLog non ancora portati)
8. RaceResults + Incidents (#251) — **fatto**, validato end-to-end
9. Championships + Standings + Academy + SeasonRecap + SkillIndex (#252) — **fatto**, validato end-to-end. Vedi nota sopra per deviazioni (championships-add/update, skill-index-snapshot) e il gap race_number chiuso. Cutover in `client.js` (#332/#349/#350) **fatto**, validato end-to-end via Chrome — dati storici migrati da Apps Script, 6 Edge Function corrette per il fix driver_id→driver_code, gap championship_name chiuso in races-list/upcoming/get. `incidents.*` ESCLUSO dal cutover, resta su Apps Script in attesa di decisione (#351) — vedi nota sopra
10. RaceRSVP + RaceCrews + TrainingInsights (#253) — **fatto**, validato end-to-end. Vedi nota sopra per deviazioni (race_crews.id uuid, is_system_account) e il gap promemoria Discord (#256)
11. Clash of Classes (#254) — **fatto**, validato end-to-end. Vedi nota sopra per la deviazione architetturale nuova (endpoint pubblici + team_slug + client service role)
12. ChampionshipInterest + PrequalCandidates + Candidates + Sponsors + Treasury + Consent (#255) — **fatto**, validato end-to-end. Vedi nota sopra per le deviazioni (amount>0 a livello DB, upsert consent via unique constraint, team_slug su consent.socialFlags) e i gap Discord/AuditLog (#256)
13. DiscordMessenger + AuditLog + Push (#256) — **fatto**, validato end-to-end. Vedi nota sopra per la deviazione (push delegato al relay Vercel, non reimplementato in Deno) e i gap Discord/audit più vecchi rimasti aperti in Races/Incidents/Championships/RaceRSVP
14. Fuel/Energy + Devices (#257) — **fatto**, validato end-to-end. Vedi nota sopra per la deviazione architetturale nuova (doppio percorso di autenticazione sessione-Supabase/device-token HMAC stateless nelle Edge Function `fuel-*`) e il gap non bloccante (`DEVICE_TOKEN_SECRET` da configurare)
15. LapData / Analisi di Passo (#258) — **fatto**, validato end-to-end. Vedi nota sopra: porting 1:1 di parser CSV, matching fuzzy nome pilota (riuso dell'algoritmo di `race-results-import`) e dedup applicativo, nessuna deviazione architetturale
16. Endurance: Auditions + Participants + Stints (#259) — **fatto**, validato end-to-end via Chrome (con un bug di riordino stint trovato e corretto durante la validazione, vedi nota sopra). Vedi nota sopra anche per la deviazione architetturale nuova (dispatcher consolidati `endurance-read`/`endurance-write` per il tetto di 100 Edge Function sul piano free)
17. Social Manager (#260) — **fatto**, validato end-to-end via Chrome. Vedi nota sopra per il dispatcher unico `social-manager` (riutilizza lo slug `endurance-auditions-get`) e i gap secrets non bloccanti
18. Race Reports + Reazioni + aggregatori Showcase/landing.data (#261) — **fatto**, validato end-to-end via Chrome. Vedi nota sopra per `laps.raceLaps` anticipato da #262, i due nuovi endpoint (`reports.update`/`reports.seedForRace`), il gap seeding chiuso in `race-results-import`, e i tre tier di auth nello stesso dispatcher consolidato
19. syncFromGarage61 + Best Lap Submissions (#262) — **fatto**, validato end-to-end via Chrome. Scope ridotto a `syncFromGarage61`+Best Lap Submissions dopo l'anticipo di `laps.raceLaps` in #261. Vedi nota sopra per la deviazione GARAGE61_TOKEN globale e il gap #179 chiuso (record-check + notifica in `best-laps-add`)
20. Pit Wall: relay Realtime per viewer remoti (#263) — **fatto**, validato end-to-end via Chrome. Vedi nota sopra per la scelta Broadcast-via-REST, la RLS su `realtime.messages` e l'accorgimento `accessToken` da riusare in #264
21. Rewire frontend reale su Supabase, mobile-first (#264) — in corso, dominio per dominio: Pubblico+Roster (#329), Calendario (#330), Best Laps/Academy/Records/Training/Submissions (#331/#342), Races/RaceResults/Championships/Standings/SkillIndex/SeasonRecap (#332/#349/#350), Clash of Classes (#333), Interest/Prequal/Candidates/Sponsors/Treasury/Consent (#334) **fatto**, validato end-to-end — vedi nota sopra per la regressione reale (nessun pilota tranne l'admin di test ha una sessione Supabase, fix fallback token legacy) e il bug indipendente `treasury_entries`. **Fix aggiuntivo (20/09/2026, segnalazione Casesi)**: stesso gap fallback trovato e chiuso anche sul dispatcher `social-manager`/`endurance-auditions-get` (Best Lap Submissions, Race Reports/Reazioni, landing.data, laps.raceLaps) — vedi nota sopra, confermato funzionante in produzione dall'utente. **Fix aggiuntivo #2 (20/09/2026, scoperto in validazione)**: lo stesso gap fallback non era mai stato chiuso sull'intero dominio #331 (`laps.list/leaderboard/update/remove`, `records.team`, `academy.ranking`, `training.insights`) — 7 Edge Function ridistribuite + `LEGACY_TOKEN_FALLBACK_ACTIONS` esteso, validato end-to-end — vedi nota sopra. **Fix #360 (20/09/2026, segnalazione Demetrio)**: `roster.updateSelf` era rimasto ESCLUSO da `SUPABASE_MIGRATED_ACTIONS` fin da #329 — ogni salvataggio "Modifica profilo" (bio/instagram/facebook/roster_track) andava sempre al vecchio backend Apps Script, invisibile lato sito perché `roster.get`/`roster.list` leggono da Supabase — vedi "Sesto bug reale" sopra per diagnosi, fix (`resolveLegacyDriver` su `roster-update-self` v3 + azione aggiunta a `SUPABASE_MIGRATED_ACTIONS`/`LEGACY_TOKEN_FALLBACK_ACTIONS`) e verifica che nessun altro pilota avesse dati persi. Messenger/AuditLog/Push (#335) **fatto**, validato end-to-end — vedi nota #335 sopra per i due bug driver_id-come-UUID trovati e corretti pre-cutover (`messenger-send`/`audit-log-list` v2) e il gap noto e accettato (nessun fallback legacy per questo dominio). Fuel/Energy/Devices/LapData (#336) **fatto**, validato end-to-end — vedi nota #336 sopra per i tre bug driver_id-come-UUID trovati e corretti pre-cutover (`lap-data-session`/`lap-data-import`/`fuel-stints` v2, il più visibile essendo la legenda del grafico Analisi di Passo) e il doppio percorso di autenticazione sessione/device-token confermato per le `fuel-*`. Endurance (#337) **fatto**, validato end-to-end in produzione (lettura + scrittura) — vedi nota #337 sopra per i quattro bug driver_id-come-UUID trovati e corretti pre-cutover (`endurance-read`/`endurance-write` v4, particolarmente critici qui perché driver_id è una vera colonna uuid con FK), il gap di auth chiuso preventivamente su `stints.list` (stesso pattern #358/#359, fallback legacy token aggiunto prima che il bug potesse mai manifestarsi live) e il ciclo di scrittura completo testato con dati di test poi rimossi. **Fix collaterale #363**: crash di `gallery_urls` in `RaceDetail.jsx` (array vs CSV string) trovato durante questa validazione e corretto — vedi nota #363 sopra. Resto di Social Manager/Showcase (#338) **fatto**, validato end-to-end in produzione (lettura + scrittura) — vedi nota #338 sopra per i sei bug driver_id-come-UUID trovati e corretti pre-cutover (due dei quali già live e mai notati: `computeRaceLaps`/`laps.raceLaps` e `showcase.summary`/`showcase.mediaKit`) e il settimo bug trovato in validazione live via Chrome (voci `UNWRAP_KEY` mancanti in `supabaseApi.js`, crash `/admin/social-manager`), oltre al ciclo di scrittura completo testato (dismiss/undismiss piano editoriale). Pit Wall (#339/#340) **fatto**, validato end-to-end — storico/nuova vista live via Chrome (vedi nota #339, incluso il fix di una regressione reale su `/roster`) e bridge C# con sessione LMU reale (vedi nota #340, incluso un bug di formato richiesta trovato e corretto nel test live). **Fix follow-up #339 (20/09/2026)**: `/pitwall-live` mostrava solo posizione/gap/miglior giro — il payload del bridge aveva sempre avuto anche giri, settori (S1/S2/S3), ultimo giro, intervallo e penalità (stesso contratto di `/pitwall` locale), semplicemente mai renderizzati dalla card. Aggiunta riga statistiche estesa con stessa convenzione viola/verde sui settori.

**#341 (rimozione fallback legacy) deliberatamente NON fatto — decisione esplicita di Demetrio il 20/09/2026.** Prima di procedere è stato verificato via query diretta lo stato reale della migrazione login: **solo 1 pilota su 28 attivi (VSD005/Demetrio) ha `auth_user_id` valorizzato** — tutti gli altri passano ancora esclusivamente dal fallback `legacy_token`/`resolveLegacyDriver` su Best Laps/Academy/Records/Training, `roster.updateSelf`, Interest/Prequal/Candidates/Sponsors/Treasury/Consent, e sul dispatcher Social Manager/Reports/Pit Wall (`endurance-auditions-get`). Rimuovere il fallback ora avrebbe bloccato (401) 27 piloti su 28 su tutte quelle funzioni — la stessa classe di regressione già vista più volte in questa fase (#334, Casesi/#331, #360), ma volontaria invece che accidentale. Presentate 3 opzioni a Demetrio (rimando / prima migro i login poi rimuovo / rimuovo comunque ora): scelto **"Rimando #341"**. Resta `pending`, da riprendere quando c'è un motivo concreto per chiuderlo — tipicamente in preparazione di #265 (spegnimento Apps Script), che richiede comunque che tutti i piloti abbiano fatto almeno un login Discord via Supabase.
22. Cutover finale: spegnimento Apps Script (#265) — richiede conferma esplicita separata dell'utente prima di procedere, data l'irreversibilità
23. Billing (Lemon Squeezy/Stripe) — solo a valle, quando i domini core funzionano

## Struttura

```
cloud/
  README.md          questo file
  schema/             migrazioni SQL, in ordine numerico
```
